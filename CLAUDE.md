# رفيق إسطنبول — دليل المشروع

هذا الملف يحوي القواعد والقرارات التي تراكمت أثناء العمل على المشروع، حتى يقدر
أي شخص أو أداة (Claude أو غيره) تفتح هذا المستودع أن تفهم السياق فورًا دون
إعادة شرح.

## طريقة التواصل مع المالك

- المالك لا يفهم الكود إطلاقًا ويتواصل بالعربية. أي شرح لتغيير يجب أن يكون
  بلغة بسيطة: ماذا يرى/يضغط عليه زائر الموقع — وليس أسماء ملفات أو diffs أو
  مصطلحات برمجية. يمكن إعطاؤه أوامر SQL جاهزة للنسخ واللصق عند الحاجة (هذا
  تنفيذ، وليس شرحًا يحتاج فهمه).
- عند تسليم mockup (HTML/CSS/JS) جاهز، يُنقل حرفيًا 1:1 (نفس الألوان، الترتيب،
  المحتوى) دون "تحسينه" ليتماشى مع تصميم الموقع الحالي.
- أي عنصر واجهة/أيقونة/asset جديد يُسحب أولاً من 21st.dev (أدوات mcp__21st__*)
  وليس من مصدر آخر، إلا إذا كان mockup جاهز مُسلَّم (تلك القاعدة تتغلب).
- الدفعات (push) بما فيها إلى main تتم مباشرة دون سؤال المستخدم في الشات أولاً.
  (قد يحجب نظام أمان داخلي عند أداة معينة أول محاولة push مباشر إلى main
  ويطلب تأكيدًا — هذا نظام مستقل، وليس خطوة إضافية يجب تكرارها يدويًا.)

## قواعد صارمة لمحتوى صفحات الخدمات

- **ممنوع** كتابة خطوات "افعلها بنفسك" (DIY) تُعلّم الزائر كيف يُنجز المعاملة
  بنفسه خطوة بخطوة (مثل تعبئة نموذج حكومي). يجوز ذكر الشروط، الأوراق المطلوبة،
  التكلفة التقريبية، المخاطر الشائعة — لكن ليس الطريقة التنفيذية الكاملة.
  السبب: رفيق خدمة مدفوعة، والمحتوى يجب ألا يُغني الزائر عن التواصل مع رفيق.
- **ممنوع** عرض أي أسعار/رسوم حكومية دقيقة في محتوى الصفحات — فقط أن التكلفة
  تتفاوت وأن رفيق يعطي الرقم الدقيق عند التواصل.
- ميزة "قراءة المزيد" يجب أن يبقى كل المحتوى موجودًا في الصفحة (لا يُحذف من
  DOM) — فقط طي بصري (max-height + تدرج) — أبدًا لا يُخفى خلف تسجيل الدخول
  لأن ذلك يدمر SEO (Google لا يسجّل دخول ليقرأ المحتوى).

## قاعدة تقنية أساسية: اتصالات Supabase/Vercel المتوفرة لبعض أدوات AI قد تكون خاطئة

بعض اتصالات MCP (Supabase وVercel) المتاحة لأدوات AI معينة على هذا الجهاز قد
تُشير إلى حساب شخص آخر وليس حساب مالك رفيق الحقيقي.
- مرجع Supabase الحقيقي لرفيق: `jdtspvkhomctqkgdmjdn` (مذكور في `.env` السطر 17
  — `VITE_SUPABASE_URL`). أي تعارض، هذا السطر هو المرجع الحاسم.
- أي حقيقة عن قاعدة البيانات يجب التحقق منها عبر تشغيل استعلام SQL يدويًا في
  لوحة Supabase (Dashboard → SQL Editor) وليس عبر اتصال آلي غير موثوق.
- أي حقيقة عن النشر (deployment) يجب التحقق منها من لوحة Vercel مباشرة، أو
  GitHub commit-status API.

## ترحيل قاعدة البيانات (migrations) يدوي دائمًا

ملفات `supabase/migrations/*.sql` لا تُطبَّق تلقائيًا بأي نظام آلي. يجب فتح
محرر SQL في لوحة Supabase ولصق الملف يدويًا (الملفات idempotent، آمنة لإعادة
التشغيل). لذلك ميزة قد تكون "منشورة بالكامل" على الموقع لكنها تفشل بصمت لأن
الجدول المرتبط بها لم يُنشأ بعد في قاعدة البيانات الحقيقية.

## دفعة (push) إلى main تُنشر تلقائيًا على Vercel

الدفع مباشرة إلى فرع `main` على GitHub (`github.com/rafeiqdev/rafiq`) يبني
**وينشر تلقائيًا** إلى الإنتاج — لا توجد خطوة "ترقية يدوية" منفصلة. أي تأخير
ملاحظ بعد الدفع هو تأخر ذاكرة التخزين المؤقت لـ CDN (بضع دقائق)، وليس مشكلة
نشر حقيقية.

## عند الإبلاغ عن تغيير "لم يظهر live"

قبل افتراض مشكلة نشر/كاش، تحقق أولاً هل التغيير موجود فعلاً في أي فرع بالمستودع
(`git log --all --grep=...`). حدثت حالات فعلية حيث اعتُقد أن تغييرًا حُفظ ودُفع،
لكنه لم يكن موجودًا في أي مكان بالمستودع إطلاقًا.

## حالة الميزات الكبيرة

- **السياحة العلاجية (Medical Tourism):** بناء كامل (قاعدة بيانات، صفحات عامة،
  طلب حجز، لوحة إدارة، مدفوعات) في `src/`، لكن ملف الترحيل
  `supabase/migrations/20260806_medical_tourism.sql` قد لا يكون مطبّقًا بعد على
  قاعدة البيانات الحقيقية — تحقق قبل الافتراض أنه يعمل.
- **مركز تحكم الإدارة (Admin Control Center):** مسار إضافي `/admin/control-center`
  في `src/admin-control-center/`، بجانب لوحة الإدارة القديمة (لم تتغير). كل
  أقسام العرض جاهزة وتقرأ بيانات حقيقية، لكن أزرار الإجراءات (موافقة/رفض/دفع
  عمولة/نشر محتوى...) معطّلة عمدًا حتى تُبنى طبقة صلاحيات + تأكيد + سجل تدقيق.
  مفعّل افتراضيًا (فلاغ kill-switch: `VITE_ADMIN_CONTROL_CENTER_ENABLED` أو
  `localStorage.rafiq_cc_enabled`).
- **أسئلة ما بعد تسجيل الدخول لكل شخصية مستخدم (`src/pages/Onboarding.tsx`):**
  تُحسَّن شخصية تلو الأخرى لإظهار أهم 3 خدمات مناسبة (هدف تجاري: بيع الاشتراك،
  بدون عرض أسعار). شخصية "الطالب" أُنجزت أولًا. الترتيب المتفق عليه: طالب ←
  وصل حديثًا ← زائر/سائح ← مقيم ← يخطط للقدوم ← مقيم منذ فترة طويلة.

## ذكاء المحادثة (AI Chat)

خاصية الدردشة (`/premium`, `/chat`) تعمل عبر دالة Vercel خادمية
(`api/ai-chat.ts`) تستدعي Google Gemini بمفتاح مطور واحد — لا تسجيل دخول من
المستخدم النهائي لأي خدمة خارجية. إن غاب المفتاح أو فشلت الدالة، يعود النظام
تلقائيًا لردود ثابتة محلية (`src/lib/ai-fallback.ts`). الخاصية هي "وكيل
استقبال" (intake agent) يجمع تفاصيل الحالة، وليست مكتب معلومات عام.
