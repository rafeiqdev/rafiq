# CHANGES — Rafiq Istanbul

## المرحلة ١ — التصميم والهوية البصرية ✅ (مكتملة)

تحسين بصري شامل دون كسر أي ميزة شغّالة، مع الحفاظ على اللغات الأربع وRTL.

### ١. نظام تصميم موحّد (Design tokens)
- **الألوان:** توحيد الهوية حول **navy `#1a3a6b`** + لون ذهبي ثانوي جديد **gold `#c69749`** (مع `dark/light/soft`)، إضافة تدرّجات navy (50/100). كلها كـ design tokens في `tailwind.config.js` ومتغيّرات CSS في `:root`.
- **المسافات/الزوايا/الظلال:** سلّم موحّد — `borderRadius` (`btn:12px`, `card:16px`, `xl2:20px`)، ظلال (`soft/card/cardHover/gold/float`)، مسافات إضافية (`18`, `11.5`).
- **الأزرار:** نظام موحّد بقاعدة `.btn` + خمسة أنواع: `btn-primary` (navy)، **`btn-gold`** (الذهبي للـ CTA الرئيسي)، `btn-secondary`، **`btn-ghost`** (شبح/إطار)، `btn-danger`، إضافة `btn-whatsapp`. مع مقاسات `btn-sm/btn-lg` وحالات كاملة: hover / active / disabled / focus-visible (حلقة تركيز).
- **البطاقات/الحقول:** توحيد `.card` و`.input` (نصف قطر موحّد، حلقة تركيز)، إضافة `.icon-chip-gold` و`.eyebrow` و`.section-title`.

### ٢. الخطوط (تحسين العربي والفارسي)
- إضافة **IBM Plex Sans Arabic** للعربية والفارسية (مع Tajawal/Vazirmatn احتياطيًا)، وبقاء **Inter** للإنجليزية والروسية.
- ضبط ارتفاع السطر للعربي: `line-height` أوسع (1.85 عام، 1.9 للفقرات) ومنع التباعد الحرفي — نص عربي مريح ومقروء لا خط لاتيني مطبّق على العربي.

### ٣. الرئيسية والتحويل
- **Hero جديد:** صورة إسطنبول حقيقية كخلفية مع طبقة navy، عنوان القيمة الواضح، **زر CTA ذهبي بارز** («احصل على المساعدة الآن» / "Get help now") + زر ثانوي «استكشف الخدمات»، و**شريط ثقة** (٤ لغات · دعم خبراء · استشارة أولى مجانية · حماية بياناتك KVKK).
- **زر «تواصل معنا»** ذهبي ثابت في الـ header (ظاهر بكل الصفحات وقبل الدخول) + نسخة كاملة العرض في قائمة الجوال.
- **زر واتساب عائم** ثابت بكل الصفحات (يقرأ `VITE_WHATSAPP_NUMBER`)، يظهر قبل الدخول، وموضعه `end-5` فينعكس تلقائيًا لأسفل-اليسار في RTL. (الربط الكامل والرسائل حسب الصفحة في المرحلة ٢.)

### ٤. الصور والأيقونات
- مجموعة أيقونات واحدة متّسقة (lucide) مسبقًا — لا إيموجي في الكود.
- إضافة صور إسطنبول حقيقية (Unsplash مُحسّنة `auto=format&q=70`) في الـ hero وقسم «من نحن»، عبر مكوّن `SiteImage` بـ **lazy-load** + تلاشٍ + **fallback** آمن (تدرّج navy) إن فشل التحميل.

### ٥. أقسام الثقة (قوالب تصميم — المحتوى يُنقّح بالمرحلة ٣)
- **«كيف نعمل»** (`HowItWorks`): ٣ خطوات ببطاقات وأيقونات ذهبية.
- **«من نحن»** (`AboutSection`): نص + نقاط مزايا + صورة إسطنبول.
- **«آراء العملاء»** (`Testimonials`): ٣ شهادات على خلفية navy (محتوى نموذجي مترجم).

### ٦. RTL والموبايل
- التحقق: لا توجد خصائص فيزيائية كاسرة للـ RTL في الكود (كله logical: `ms/me/ps/pe/start/end`).
- أهداف لمس: زر واتساب 56px، أزرار الـ hero 48px، زر التواصل بالجوال 44px.
- تم التحقق بصريًا من الإنجليزية والعربية (RTL) — انعكاس كامل صحيح للهيدر والـ hero وأقسام الثقة وزر الواتساب.

---

### الملفات المعدّلة / المضافة

**إعدادات وأسس:**
- `‎.env` *(جديد)* — `VITE_WHATSAPP_NUMBER` (قيمة مؤقتة `905000000000` — استبدلها برقمك) و`VITE_CONTACT_EMAIL`.
- `tailwind.config.js` — gold + tokens (ألوان/زوايا/ظلال/مسافات).
- `index.html` — خط IBM Plex Sans Arabic.
- `src/index.css` — خطوط RTL + ارتفاع السطر، نظام الأزرار الكامل، helpers، أنيميشن `float-pulse`.

**مكوّنات جديدة:**
- `src/lib/images.ts` — روابط صور إسطنبول المحسّنة.
- `src/components/SiteImage.tsx` — صورة محسّنة lazy + fallback.
- `src/components/WhatsAppButton.tsx` — زر الواتساب العائم.
- `src/components/sections/HowItWorks.tsx`
- `src/components/sections/AboutSection.tsx`
- `src/components/sections/Testimonials.tsx`

**تعديلات:**
- `src/components/Layout.tsx` — زر «تواصل معنا» في الهيدر + قائمة الجوال + تركيب زر الواتساب.
- `src/pages/Home.tsx` — hero جديد (صورة + CTA ذهبي + شريط ثقة) + دمج أقسام HowItWorks/About/Testimonials.
- `src/i18n/locales/{en,ar,ru,fa}.json` — مفاتيح جديدة: `nav.contact`, `whatsapp.*`, `home.heroCtaPrimary/Secondary`, `home.trustbar.*`, `home.how.*`, `home.about.*`, `home.testimonials.*` (٦٢٤ مفتاحًا/لغة، تطابق كامل).

### التحقق
- ✅ `npm run build` ينجح (0 أخطاء TypeScript).
- ✅ `npm run check:i18n` — تطابق كامل ٦٢٤ مفتاحًا في en/ar/ru/fa.
- ✅ تحقّق بصري: الإنجليزية + العربية (RTL)، الـ hero/أقسام الثقة/زر الواتساب/زر التواصل.
- ✅ لم تُكسر أي ميزة (المصادقة/الجلسات/leads/الإحالات/الكتل الشرطية/البحث/الأسئلة الشائعة سليمة).

### ملاحظات
- ⚠️ `VITE_WHATSAPP_NUMBER` قيمة مؤقتة — ضع رقمك الحقيقي في `.env` (بصيغة دولية بدون +).
- بعد أي تعديل على `tailwind.config.js` يجب إعادة تشغيل خادم التطوير (`npm run dev:all`) — لا يُعاد تحميله تلقائيًا.
- محتوى أقسام الثقة (الشهادات/الفريق) نموذجي وسيُنقّح في المرحلة ٣.
- `src/components/RateTape.tsx` لم يعد مستخدمًا (حلّ محله `TopRatesBar` الأسود سابقًا) — يمكن حذفه لاحقًا.

---

### تعديلات لاحقة على المرحلة ١ (بطلب المستخدم)
- **الصفحة الرئيسية:** إزالة كل الأزرار من الـ hero (زر «احصل على المساعدة» + شارة «باقتك»)، واستبدال الصورة الواحدة بـ **سلايدر صور تركيا في الخلفية** يتبدّل تلقائيًا كل ٣ ثوانٍ (٤ صور مؤكَّدة، انتقال crossfade سلس، يعمل بنفس الاتجاه في RTL) — مكوّن جديد `ImageCarousel`. أُبقي العنوان والوصف وشريط الثقة والبحث.
- **بانرات بالصور:** مكوّن جديد موحّد `PageHero` (صورة + طبقة navy + عنوان) طُبِّق على **الإقامة** و**حيل إسطنبول** و**العقارات** بدل بانر الـ SVG — مظهر نظيف ومتّسق عبر الصفحات.
- **صور العقارات:** تحديث صور العروض المزروعة إلى صور عقارية حقيقية، وتقوية `ListingImage` بسلسلة fallback (صورة العرض → صورة عقارية منسّقة حسب الترتيب → تدرّج) لضمان ظهور صورة دائمًا.
- **إزالة سابقة:** زر واتساب العائم وزر «تواصل معنا» في الهيدر (بطلب المستخدم).
- ملف صور جديد `src/lib/images.ts` (CAROUSEL / BANNERS / LISTING_PHOTOS — كلها روابط Unsplash محسّنة ومتحقَّق من تحميلها)، ومكوّن `SiteImage` للتحميل الكسول + fallback.
- ⚠️ تمت إعادة تهيئة قاعدة بيانات العرض لتحديث صور العقارات (حساب الأدمين يُعاد إنشاؤه تلقائيًا: **admin/admin**).
- ملاحظة عن «green»: أبقيت طبقة التغطية **navy** للحفاظ على هوية الموقع (navy + ذهبي). إن كنت تقصد طبقة **خضراء** فعليًا، أخبرني وأبدّلها في ثانية واحدة.
- **بطاقات «استكشف كل ما يقدّمه رفيق» (الصفحة الرئيسية):** أُعيد تصميمها كبطاقات بخلفية صورة لكل موضوع (إقامة = مستندات، عقارات = مبنى، سياحة علاجية = مستشفى، أدلة = كتب، حيل = مدينة إسطنبول...) مع تدرّج navy فوق الصورة، أيقونة زجاجية، عنوان ووصف و«اعرف المزيد» ذهبي، وتكبير خفيف عند المرور. الصور في `EXPLORE_PHOTOS` بملف `src/lib/images.ts` (متحقَّق من تحميلها).

- **صفحة المساعدة الجديدة `/help`:** بدل توجيه «ساعدني في هذا» إلى الشات المغلق/غير المستجيب، صارت كل أزرار «ساعدني في هذا» (في الكتل وبطاقات الإقامة) تفتح صفحة مساعدة ودّية:
  - مشهد متحرّك: خلفية صورة إسطنبول حقيقية + **سيارة بيضاء (طراز Fiat Egea)** تدخل بحركة وعجلاتها تدور + **مستشار** يلوّح (رسوم SVG، مكوّن `AdvisorScene`، أنيميشن `driveIn/wheelSpin/riseIn`).
  - عنوان ونص داعم لطيف.
  - خياران: **«نعم، أريد المساعدة»** (يفتح حجز موعد مجاني مع خبير بشري) و **«تحدّث مع المساعد الذكي»** (إلى `/premium`).
  - يحترم `prefers-reduced-motion`، ويعمل في الاتجاهين (المشهد مثبّت LTR كي لا تنعكس حركة السيارة).
  - الموضوع يُمرَّر عبر `?topic=` ويُرفق في ملخص الحجز ليصل للأدمين.
  - **تحديث المشهد بصور حقيقية:** استُبدلت رسوم الـ SVG بصور حقيقية — **سيارة المستخدم (Fiat Egea بيضاء) بعد إزالة الخلفية** + **مستشار حقيقي (صورة)**، على خلفية **بيضاء مع علم تركيا**. أُزيلت خلفية السيارة محليًا ومجانًا عبر `@imgly/background-removal-node` (مجلد `bgtool` خارج التطبيق؛ Higgsfield كان بلا رصيد). الأصول في `public/scene/` (`car.png` شفافة + `person-source.jpg`). السيارة لا تزال تتحرك (driveIn) فوق ظلّ أرضي.

## المرحلة ٢ — التواصل والتحويل
⏳ بانتظار الموافقة.

## المرحلة ٣ — التفعيل والمحتوى
⏳ بانتظار الموافقة.
