# لوحة المستخدم المسجّل — تشخيص وإعادة تصميم

الملف المعني: `src/pages/UserHome.tsx` (392 سطر) — الصفحة التي يراها المستخدم بعد تسجيل الدخول على `/`.

---

## الجزء الأول — لماذا هي مملّة فعلاً

هذه ليست انطباعات عامة. كل نقطة مأخوذة من الكود مع رقم السطر.

### ١. العنوان نفسه مكرَّر مرتين في نفس الشاشة

```tsx
// السطر 226 — بطاقة التقدّم
<h2>{t('dash.journeyTitle')}</h2>

// السطر 260 — بطاقة قائمة المهام
<h2>{t('dash.journeyTitle')}</h2>
```

بطاقتان متتاليتان بنفس العنوان تمامًا («مسيرتي»)، إحداهما شريط تقدّم والأخرى قائمة من نفس
البيانات. المستخدم يقرأ العنوان مرتين خلال تمريرة واحدة فيشعر أن الصفحة تعيد نفسها.
**هذا خطأ وليس رأيًا — يجب دمج البطاقتين أو تغيير عنوان الثانية.**

### ٢. ثمانية صناديق بيضاء متطابقة في عمود واحد

كل قسم يستخدم نفس المكوّن بلا أي استثناء:

```tsx
function Panel({ children, className }) {
  return <section className="rounded-2xl border border-gray-200 bg-white p-5">…</section>;
}
```

النتيجة: تقدّم، خطوة تالية، مهام، تجديدات، حجوزات، إشعارات، خدمات، أدلة، إحالة — تسعة
عناصر بنفس اللون ونفس الحدود ونفس الحشوة ونفس نصف القطر، مرصوصة عموديًا.

لا يوجد **تسلسل بصري**: لا شيء يقول للعين «ابدأ من هنا». المستخدم يمسح الشاشة فيرى نسيجًا
واحدًا متكرّرًا، وهذا هو التعريف الدقيق للملل البصري.

### ٣. عمود واحد بعرض 768 بكسل على شاشة 1920

```tsx
<div className="mx-auto max-w-3xl px-4 py-8">
```

على سطح المكتب يبقى ٦٠٪ من الشاشة فارغًا يمينًا ويسارًا، والمحتوى شريط ضيّق طويل يتطلّب
تمريرًا كثيرًا. تبدو كأنها تطبيق هاتف مُمدَّد لا لوحة تحكّم. لوحات التحكّم الحقيقية تستخدم
شبكة.

### ٤. ثلاثة أزرار تؤدي إلى نفس المكان

```tsx
السطر 238:  <Link to="/journey">  {t('dash.continueCta')}   // متابعة
السطر 250:  <Link to={next.relatedRoute || '/journey'}>      // غالبًا /journey
السطر 261:  <Link to="/journey">  {t('dash.viewAll')}        // عرض الكل
```

ثلاثة نداءات فعل مرئية في آنٍ واحد تذهب إلى `/journey`. هذا يشتّت القرار بدل أن يوجّهه،
ويجعل الصفحة تبدو كوسيط لا كوجهة.

### ٥. الصفحة تصبح **أفرغ** كلما كان المستخدم أحدث

أربعة أقسام مشروطة بوجود بيانات:

```tsx
{renewals.length > 0 && …}        // تجديدات
{myBookings.length > 0 && …}      // حجوزات
{notifs.length > 0 && …}          // إشعارات
{relatedServices.length > 0 && …} // خدمات
```

المستخدم الجديد — وهو الأكثر احتياجًا للتوجيه — يرى: ترويسة، تقدّم، خطوة تالية، ٣ مهام،
زر أدلة، بطاقة إحالة. هذا كل شيء. **المنطق معكوس**: من يحتاج المساعدة أكثر يحصل على
أقل محتوى.

### ٦. بطاقة كاملة لزرّ واحد

```tsx
<Panel className="mt-4">
  <h2>{t('dash.guides')}</h2>
  <Link to="/hub" className="btn-secondary">{t('nav.hub')}</Link>
</Panel>
```

صندوق أبيض بحجم كامل يحتوي على عنوان وزر. صفر محتوى. كان يمكن أن يعرض ثلاثة عناوين أدلة
مقترحة حسب حالة المستخدم.

### ٧. لا يوجد أي سبب للعودة غدًا

الشيء الوحيد المتغيّر في الصفحة هو نسبة التقدّم — وهي لا تتحرك إلا إذا ضغط المستخدم بنفسه
على مهمة. **لا يوجد أي محتوى يتجدّد تلقائيًا.** لا أخبار، لا أسعار، لا مواعيد قادمة،
لا تنبيهات زمنية. المستخدم يفتح الصفحة فيجدها كما تركها بالضبط، فيتوقف عن فتحها.

### ٨. أهم ميزتين مدفوعتين غائبتان تمامًا عن اللوحة

`/premium` (المساعد الذكي) و`/map` (الخريطة) هما ما يدفع المستخدم مقابله — ولا يوجد أي
مدخل لهما في `UserHome.tsx`. المشترك لا يرى ما يدفع مقابله كل شهر، والمجاني لا يرى ما يفوته.
هذا يضر **الاحتفاظ** و**الترقية** في آن واحد.

### ٩. ميزات مبنية بالفعل وغير مستخدَمة

- **البثّ الإداري**: يوجد `GET /api/admin/news` و`POST /api/admin/news` (جدول `broadcasts`)
  — قناة جاهزة لإخبار المستخدمين بتغيّر نظامي أو حيّ أُغلق للتسجيل. لا تظهر في اللوحة.
- **أسعار الصرف**: `GET /api/rates` مع مزامنة يومية — أكثر معلومة يفتحها الأجنبي في تركيا
  يوميًا. موجودة في شريط الضيوف فقط، غائبة عن لوحة المسجّل.
- **خزنة المستندات**: `/api/documents` مبنية بالكامل. لا يوجد لها أي مدخل من اللوحة.

### ١٠. تفاصيل أصغر

- التحية `dash.greeting` ثابتة ولا تراعي الوقت (صباح/مساء) ولا آخر زيارة.
- بطاقة «ادعُ واربح» — الشيء الوحيد الذي يربّح المستخدم مالًا — في **آخر** الصفحة.
- الباقة الحالية غير معروضة في أي مكان؛ المستخدم لا يعرف إن كان مجانيًا أم برو.
- جرس الإشعارات مرسوم داخل الصفحة (السطر 220) بينما الترويسة العامة ترسم جرسًا آخر على
  سطح المكتب — جرسان في شاشة واحدة.
- التجديدات تعرض «متبقٍ ١٢ يومًا» بلا أي زرّ إجراء. معلومة قلق بلا مخرج.
- لا توجد أي حالة احتفال عند إكمال ١٠٠٪ — المستخدم ينهي رحلته فلا يحدث شيء.

---

## الجزء الثاني — إعادة التصميم المقترحة

### المبدأ الحاكم

اللوحة الحالية تجيب على سؤال **«أين وصلت؟»** فقط. اللوحة الجيدة تجيب على ثلاثة أسئلة
بالترتيب:

1. **ما الذي تغيّر منذ آخر زيارة؟** ← يعطي سببًا للفتح
2. **ما الشيء الواحد الذي أفعله الآن؟** ← يعطي وضوحًا
3. **ماذا يوجد هنا أيضًا؟** ← يعطي اكتشافًا

### البنية المقترحة (سطح المكتب — شبكة عمودين)

```
┌─────────────────────────────────────────────────────────────┐
│  صباح الخير يا علي 👋            [برو ✦]  [🔔 3]  [الصورة]  │  ترويسة مضغوطة
│  طالب · انضممت منذ 3 أشهر · تقدّمك 62%                       │
└─────────────────────────────────────────────────────────────┘

┌──────────────── العمود الأساسي (2fr) ────┐ ┌── الجانبي (1fr) ──┐
│                                          │ │                   │
│  ★ الخطوة التالية                        │ │  ⚡ اليوم           │
│  ─────────────────────────               │ │  ─────────────    │
│  سجّل عنوانك بعقد إيجار موثّق             │ │  USD 47.34 ↑      │
│  المستندات المطلوبة: 3                   │ │  EUR 53.92 ↓      │
│  الوقت المتوقع: يومان                    │ │                   │
│  [ابدأ الآن →]  [اسأل رفيق 💬]           │ │  📣 تنبيه رفيق     │
│                                          │ │  الفاتح أُغلق      │
├──────────────────────────────────────────┤ │  للتسجيل مؤقتًا     │
│  مسيرتي                          62%     │ │                   │
│  ▓▓▓▓▓▓▓▓▓▓▓▓░░░░░░░  5 من 8            │ │  ⏰ مواعيدك        │
│  ✓ الرقم الضريبي                         │ │  موعد e-İkamet     │
│  ✓ التأمين الصحي                         │ │  بعد 4 أيام        │
│  ● تسجيل العنوان        ← التالية        │ │  [التفاصيل]       │
│  ○ موعد e-İkamet                         │ │                   │
│  [كل المهام ←]                           │ │  🔴 تجديدات        │
├──────────────────────────────────────────┤ │  الإقامة: 21 يومًا │
│  اقرأ قبل خطوتك التالية                  │ │  [جدّد الآن →]     │
│  ┌──────────┬──────────┬──────────┐      │ │                   │
│  │ عقد إيجار │ الأحياء   │ الترجمة  │      │ │  🎁 ادعُ واربح     │
│  │ 4 دقائق   │ 6 دقائق  │ 3 دقائق  │      │ │  ربحت 450 ل.ت     │
│  └──────────┴──────────┴──────────┘      │ │  [شارك الرابط]    │
└──────────────────────────────────────────┘ └───────────────────┘
```

على الهاتف: نفس الترتيب عموديًا، مع «اليوم» و«مواعيدك» مباشرة بعد «الخطوة التالية».

---

## الجزء الثالث — الاقتراحات مرتّبة حسب الأثر

### الفئة أ — إصلاحات فورية (أثر عالٍ، جهد ساعة أو أقل)

| # | الاقتراح | لماذا |
|---|---------|-------|
| أ١ | **دمج بطاقتَي «مسيرتي»** في بطاقة واحدة: شريط التقدّم + قائمة المهام معًا | يزيل التكرار الصارخ ويقصّر الصفحة ٢٠٪ |
| أ٢ | **حذف زرّي «متابعة» و«عرض الكل»** والإبقاء على واحد | ثلاثة أزرار لنفس الوجهة تشوّش القرار |
| أ٣ | **رفع بطاقة «الخطوة التالية» إلى الأعلى** قبل شريط التقدّم | الفعل قبل التقرير — المستخدم يريد أن يعرف ماذا يفعل لا كم أنجز |
| أ٤ | **عرض شارة الباقة** بجانب الاسم (`مجاني` / `برو` / `إيليت`) | يذكّر المشترك بقيمة ما يدفعه ويذكّر المجاني بما يفوته |
| أ٥ | **تحية حسب الوقت** — صباح/مساء الخير | تكلفة صفر، تجعل الصفحة تبدو حيّة |
| أ٦ | **إزالة جرس مكرّر** على سطح المكتب | جرسان في شاشة واحدة |
| أ٧ | **زرّ إجراء مع كل تجديد**: «جدّد الآن» / «اطلب مساعدة» | لا تُقلق المستخدم بلا مخرج |

### الفئة ب — إعطاء سبب للعودة (أثر عالٍ جدًا)

| # | الاقتراح | التنفيذ |
|---|---------|--------|
| ب١ | **بطاقة «اليوم»**: سعر الدولار واليورو مقابل الليرة مع اتجاه التغيّر | `GET /api/rates` موجود بالفعل — استدعاء واحد |
| ب٢ | **بطاقة «تنبيهات رفيق»**: بثّ إداري قصير (حي أُغلق، رسوم تغيّرت، مهلة قاربت) | جدول `broadcasts` و`/api/admin/news` **مبنيان بالكامل وغير مستخدَمين** |
| ب٣ | **عدّاد تنازلي للمواعيد**: «موعدك بعد ٤ أيام» بدل تاريخ ثابت | الحجوزات موجودة، تحتاج حساب فرق زمني فقط |
| ب٤ | **«ماذا حدث منذ آخر زيارة»**: خط زمني قصير بالتغيّرات | يحوّل الصفحة من مرآة ساكنة إلى موجز |
| ب٥ | **تذكير موسمي**: «شتاء إسطنبول — تحقق من فاتورة التدفئة» | محتوى متجدّد بجهد تحريري بسيط |

### الفئة ج — إبراز الميزات المدفوعة (أثر مباشر على الإيراد)

| # | الاقتراح |
|---|---------|
| ج١ | **زر «اسأل رفيق» داخل بطاقة الخطوة التالية** مع سؤال مقترح جاهز: «ما المستندات المطلوبة لتسجيل العنوان؟» — يفتح `/premium` بالسؤال مملوءًا |
| ج٢ | **بطاقة خريطة مصغّرة**: ٣ أماكن قريبة من حي المستخدم (لمشترك برو)، أو نسخة ضبابية مع «افتح ٥٠+ مكانًا موثوقًا» (للمجاني) |
| ج٣ | **مدخل خزنة المستندات**: «٣ من ٥ مستندات مرفوعة» مع شريط تقدّم صغير — الميزة مبنية وغير مرئية |
| ج٤ | **للمستخدم المجاني**: صفّ واحد أنيق يعرض ما فتحه هذا الأسبوع مقابل ما هو مقفل — بلا إلحاح |
| ج٥ | **للمشترك**: «قيمة اشتراكك هذا الشهر: ١٢ سؤالًا، موعدان مجانيان، ٣ أدلة» — يقلّل الإلغاء بشكل مثبت |

### الفئة د — التسلسل البصري

| # | الاقتراح |
|---|---------|
| د١ | **ثلاثة مستويات للبطاقات** بدل واحد: بارزة (خطوة تالية — خلفية ملوّنة وحدّ سميك)، عادية (بيضاء)، خفيفة (بلا حدّ، خلفية كريمية) |
| د٢ | **شبكة عمودين على سطح المكتب** (`lg:grid-cols-3` بنسبة 2:1) بدل `max-w-3xl` — يقلّل التمرير للنصف ويملأ الشاشة |
| د٣ | **بطاقات كثافة مختلفة**: التجديدات صفوف مضغوطة، الأدلة بطاقات بصور، الإحالة شريط أفقي |
| د٤ | **شريط تقدّم دائري** بدل الخطي في الترويسة — أوضح وأصغر |
| د٥ | **ألوان الحالة**: أخضر للمنجَز، كهرماني للقريب، أحمر للمتأخر — موجودة في التجديدات فقط، عمّمها |
| د٦ | **رسوم متحرّكة دقيقة**: شريط التقدّم يتحرّك عند الدخول، البطاقات تظهر بتتابع 40 مللي ثانية |

### الفئة هـ — التخصيص

| # | الاقتراح |
|---|---------|
| هـ١ | **حسب الحالة**: الطالب يرى «معادلة الشهادة»، المستثمر يرى «الطابو والجنسية»، العائلة ترى «تسجيل المدارس» |
| هـ٢ | **حسب مدة الإقامة**: الوافد الجديد يرى «أول ٣٠ يومًا»، المقيم منذ سنة يرى «التجديد» |
| هـ٣ | **حسب اللغة**: المستخدم الروسي يرى محتوى وخدمات موجّهة للجالية الروسية |
| هـ٤ | **حسب الحيّ**: بعد معرفة حيّ المستخدم — «٣ مكاتب ترجمة محلّفة في الفاتح» |
| هـ٥ | **حسب الوقت**: تنبيه الموعد يظهر بارزًا في اليوم السابق ثم يختفي |

### الفئة و — لحظات إنسانية

| # | الاقتراح |
|---|---------|
| و١ | **احتفال عند ١٠٠٪**: شاشة تهنئة + «رحلتك اكتملت — إليك ما بعدها» |
| و٢ | **احتفال عند كل معلم**: إنجاز الإقامة أو فتح الحساب البنكي حدث حقيقي في حياة الشخص |
| و٣ | **إظهار الوقت الموفَّر**: «وفّرت ~٦ ساعات و٣ زيارات مكاتب» — يُحسب من المهام المنجزة |
| و٤ | **حالة فارغة كريمة**: بدل صندوق فارغ، «لا حجوزات بعد — احجز مكالمة مجانية مع خبير» |
| و٥ | **ذكرى سنوية**: «مرّ عام على وصولك إلى إسطنبول 🎉» |

### الفئة ز — إجراءات سريعة

| # | الاقتراح |
|---|---------|
| ز١ | **صفّ أزرار سريعة تحت الترويسة**: اسأل رفيق · احجز مكالمة · ارفع مستندًا · اطلب خدمة |
| ز٢ | **إكمال المهمة من اللوحة مباشرة** بدل الانتقال إلى `/journey` |
| ز٣ | **بحث شامل** (`Ctrl/Cmd + K`) — يوجد بحث في صفحة الضيوف وغائب عن اللوحة |
| ز٤ | **زرّ واتساب عائم** ثابت — قناة التواصل المفضّلة لهذا الجمهور |

### الفئة ح — أداء ومقاييس

| # | الاقتراح |
|---|---------|
| ح١ | **تحميل متوازٍ**: `bookingsApi.mine()` و`notificationsApi.list()` تعملان بالتسلسل — استخدم `Promise.allSettled` |
| ح٢ | **هيكل تحميل مطابق للتخطيط** بدل مستطيلات عامة، لتقليل `layout shift` |
| ح٣ | **قياس ما يُنقر فعلًا** على اللوحة — بيانات تحسم النقاش بدل الآراء |
| ح٤ | **تخزين مؤقت للأسعار والبثّ** في `localStorage` لعرض فوري ثم تحديث بالخلفية |

---

## الجزء الرابع — لو نفّذت خمسة أشياء فقط

بالترتيب من الأعلى أثرًا:

1. **دمج بطاقتَي «مسيرتي»** ورفع «الخطوة التالية» إلى القمة — يزيل أوضح شعور بالتكرار.
2. **بطاقة «اليوم»** بأسعار الصرف + بثّ رفيق — أول سبب حقيقي للعودة يوميًا.
3. **شبكة عمودين** على سطح المكتب مع ثلاثة مستويات بصرية — يزيل الرتابة فورًا.
4. **زرّ «اسأل رفيق» داخل الخطوة التالية** بسؤال جاهز — يربط اللوحة بالميزة المدفوعة.
5. **حالات فارغة تدعو لفعل** بدل إخفاء الأقسام — المستخدم الجديد لا يعود يرى صفحة شبه خالية.

---

## ملاحظة أخيرة

لا يوجد خطأ في هندسة هذه الصفحة — الحالات مُدارة جيدًا (`loading` / `error` / `empty` / `guest`)،
والأقسام الثانوية تفشل بصمت دون إسقاط اللوحة، والتعليقات تشرح القرارات بوضوح. المشكلة
تصميمية بحتة: **كل شيء بُني بنفس الوزن البصري ونفس المصدر (الرحلة فقط)**. إضافة مصدر بيانات
متجدّد واحد — الأسعار أو البثّ — وتمييز بطاقة واحدة بصريًا سيغيّر الإحساس بالصفحة أكثر مما
تتوقّع.
