# تقرير تدقيق موقع Rafiq Istanbul

> تقرير شامل مبني على قراءة الكود الفعلي (مكوّنات React، ملفات المسارات، ملفات الترجمة، والخادم) — وليس على أسماء الملفات.

**نظرة سريعة:** تطبيق ويب متعدّد اللغات (عربي/إنجليزي/روسي/فارسي) لخدمات الأجانب في إسطنبول. الواجهة React 18 + TypeScript + Vite + Tailwind، والخلفية Express 5 + SQLite. 24 مسارًا، 19 ملف صفحة، 588 مفتاح ترجمة بتطابق كامل عبر اللغات الأربع.

---

## الفهرس
1. الصفحات والمسارات
2. محتوى كل صفحة
3. الميزات التفاعلية
4. حالة التنفيذ
5. اللغات الأربع
6. التواصل والتحويل
7. البنية التقنية

---

## 1. الصفحات والمسارات

| المسار | اسم المكوّن | ملخص حقيقي عن محتوى الصفحة |
|--------|-------------|----------------------------|
| `/` | `Home` | الصفحة الرئيسية (محمّلة مباشرة لا كسولًا). تعرض قسم Hero بعنوان "رفيقك الذكي في إسطنبول" ونص فرعي يشرح المساعدة في الإقامة والعقارات والسياحة العلاجية، مع بطاقات الخدمات وأزرار CTA الداخلية. |
| `/auth` | `Auth` | صفحة تسجيل الدخول / إنشاء الحساب. تتضمّن زر Google (GIS حقيقي إذا ضُبط `GOOGLE_CLIENT_ID` وإلا زر "dev") إضافة إلى المصادقة بالبريد. |
| `/pricing` | `Pricing` | صفحة الباقات (Light / Pro / Elite) مع المزايا والأسعار، وأزرار تقود إلى تدفّق الدفع. |
| `/checkout` | `Checkout` | صفحة إتمام الدفع. تعرض خيارات الدفع (بطاقة عبر "dev gateway simulator" أو تحويل بنكي مع IBAN) وملخّص الطلب. |
| `/smart` | `Smart` | صفحة "المساعد الذكي" المجاني/الأساسي — واجهة دردشة تعتمد مساعد AI مع fallback حتمي ما لم يُضبط `ANTHROPIC_API_KEY`. |
| `/premium` | `Premium` | واجهة الدردشة المدفوعة (Premium) مع المساعد الذكي وميزات الباقات الأعلى. |
| `/chat` | `Premium` | مسار بديل (alias) يعرض نفس مكوّن `Premium` بالضبط. |
| `/map` | `MapPage` | صفحة الخريطة التفاعلية (react-leaflet) لعرض المواقع والخدمات على خريطة إسطنبول. |
| `/referrals` | `Referrals` | صفحة برنامج الإحالة — رمز الإحالة الخاص بالمستخدم وروابط المشاركة وتتبّع المكافآت. |
| `/residency` | `Residency` | صفحة خدمة الإقامة (residency) — معلومات وخطوات وأزرار طلب الخدمة (leads). |
| `/real-estate` | `RealEstate` | صفحة العقارات — قوائم عقارية وأزرار طلب الخدمة "Request" (leads) للتواصل بخصوص العقار. |
| `/health-tourism` | `HealthTourism` | صفحة السياحة العلاجية — الخدمات الطبية وأزرار طلب الخدمة "Request" (leads). |
| `/tricks` | `Tricks` | صفحة "الحيل/النصائح" — مجموعة إرشادات ونصائح عملية للأجانب في إسطنبول. |
| `/hub` | `Hub` | صفحة الدليل/المركز المعرفي (Hub) — قائمة المقالات أو الأدلّة الإرشادية. |
| `/hub/:slug` | `HubDetail` | صفحة تفاصيل مقال/دليل واحد داخل الـ Hub، تُحدَّد عبر معامل `:slug` (مُصدَّرة من نفس ملف `Hub`). |
| `/profile` | `ProfilePage` | صفحة ملف المستخدم — البيانات الشخصية والباقة الحالية وإدارة الحساب. |
| `/notifications` | `Notifications` | صفحة الإشعارات — قائمة التنبيهات مع عدّاد غير المقروء (`unread`) الظاهر في الهيدر. |
| `/admin` | `Admin` | لوحة الأدمين (تظهر للمستخدم `isAdmin` فقط). الحساب المزروع تلقائيًا: `rafeiq.dev@gmail.com` / `admin`، مع إمكانية تجاوز أسعار العملات. |
| `/admin/bookings` | `AdminBookings` | صفحة إدارة الحجوزات للأدمين، مع شارة عدّاد الحجوزات الجديدة (`newBookings`) في الهيدر. |
| `/terms` | `Legal` (`doc="terms"`) | صفحة قانونية تعرض وثيقة "الشروط والأحكام" عبر المكوّن المشترك `Legal`. |
| `/privacy` | `Legal` (`doc="privacy"`) | صفحة قانونية تعرض وثيقة "سياسة الخصوصية". |
| `/refund` | `Legal` (`doc="refund"`) | صفحة قانونية تعرض وثيقة "سياسة الاسترداد". |
| `/r/:code` | `ReferralLanding` | صفحة هبوط الإحالة: تلتقط رمز الإحالة من `:code`، تخزّنه في `localStorage` (`rafiq_ref` بحروف كبيرة) وتسجّل النقرة عبر `referrals.click`، ثم تعيد التوجيه فورًا إلى `/`. الوحيدة خارج غلاف `Layout`. |
| `*` | `NotFound` | صفحة "غير موجود" (404) لأي مسار غير معرّف. |

ملاحظة: يوجد أيضًا مكوّن `ReferralQueryCapture` (ليس مسارًا) يلتقط الإحالة من باراميتر الاستعلام `?r=CODE` على أي رابط هبوط ويسجّلها بنفس آلية `localStorage` + `referrals.click`.

### غلاف Layout والمكوّنات المحيطة

- **غلاف `Layout`**: يلفّ كل المسارات عدا `/r/:code`. يتكوّن من ثلاث طبقات: (1) **شريط الأسعار العلوي** `TopRatesBar` أعلى الصفحة لعرض أسعار الصرف؛ (2) **الهيدر** اللاصق (`sticky top-0`) الذي يضم الشعار `Logo`، شريط التنقّل الرئيسي (الرئيسية، الذكي، Premium، الخريطة، الدليل، الأسعار) وقائمة منسدلة "الخدمات" `ServicesMenu` (الحيل، الإقامة، العقارات، السياحة العلاجية، الإحالات)، إضافة إلى جرس الإشعارات بعدّاد `unread`، مبدّل اللغة `LangSwitcher`، وزر الدخول/الملف الشخصي مع اسم الباقة، وقائمة جوّال منسدلة، وروابط الأدمين الحمراء `/admin` و`/admin/bookings` للمستخدم الأدمين فقط؛ (3) **الفوتر** على خلفية navy داكنة فيه الشعار والشعار النصّي وروابط الخدمات والروابط القانونية (الشروط، الخصوصية، الاسترداد) وحقوق النشر. المحتوى يُعرض داخل `<Outlet/>` مع تأثير دخول `route-fade` يُعاد تشغيله عند كل تنقّل (عبر `key={location.pathname}`). كما يضبط الهيدر عنوان المستند و`meta description` حسب اللغة.
- **بوّابة `LanguageSelector`**: في `Shell` يُتحقَّق من `langSelected`؛ فإذا لم تُختَر اللغة بعد، تُعرض شاشة اختيار اللغة وحدها وتُحجب بقية التطبيق حتى يختار المستخدم لغته.
- **`OnboardingModal`**: يظهر فوق المحتوى إذا لم يكتمل الـ onboarding (`!onboarded`).
- **`ErrorBoundary`**: يلفّ التطبيق كاملًا في `App` (حول `AppProvider` و`BrowserRouter`) لالتقاط أخطاء العرض ومنع انهيار الواجهة بالكامل.
- **التحميل الكسول (lazy)**: كل المسارات الثانوية محمّلة عبر `React.lazy` + `Suspense` لتقليل حجم الحزمة الأولية (تعليق `P3-7`)، بينما `Home` وحدها مستوردة مباشرةً. أثناء التحميل يُعرض مكوّن `Spinner` (دائرة دوّارة مع `role="status"`) كـ fallback.

---

## 2. محتوى كل صفحة

### Home — المسار `/`

الترتيب الفعلي للأقسام كما تُعرَض (render) في `src/pages/Home.tsx`:

1. **قسم الـ Hero (ترويسة البطل)** — في الأعلى زر دائري داكن يعرض «باقتك: ...» (`home.yourPlan` = «باقتك» متبوعًا باسم الباقة الحالية أو «مجاني»)، يقود عند النقر إلى `/pricing`. تحته العنوان الرئيسي الكبير: **«كل ما تحتاجه لإسطنبول في مكان واحد»** (`home.heroTitle`)، وسطر تعريفي تحته: «الإقامة والبنوك والسكن والصحة والحياة اليومية — خطوة بخطوة وبلغتك.» (`home.heroSubtitle`). ثم حقل بحث بعبارة إرشادية «ابحث في الأدلة والخدمات...» (`home.searchPlaceholder`) مع أيقونة بحث؛ البحث يُصفّي بطاقات الخطة المخصصة فوريًا.

2. **خطتك المخصصة** (`home.forYou` = «خطتك المخصصة») — عنوان القسم وبجانبه زر ثانوي «تغيير إجاباتي» (`home.editAnswers`) يعيد فتح أسئلة الـ onboarding. ثم شبكة بطاقات (`BlockCard`) ديناميكية تُولَّد من ملف المستخدم عبر `blocksFor(profile)` وتُرتَّب حسب الأولوية. أمثلة العناوين الفعلية لهذه البطاقات (تختلف بحسب مسار المستخدم planning/arrived/living/browsing والعائلة): «قبل السفر: التأشيرة والمستندات»، «باقة الوصول: أول 48 ساعة»، «احصل على شريحة تركية SIM / eSIM»، «إسطنبول كارت والتنقل»، «احصل على رقمك الضريبي (Vergi No)»، «افتح حسابًا بنكيًا تركيًا»، «تصريح الإقامة (İkamet)»، «متتبع التجديدات»، «حاسبة تكلفة المعيشة»، «دليل مدينة إسطنبول»، «خدماتنا»... كل بطاقة تحوي أيقونة وعنوان ووصف، وقد تحتوي حسب نوعها على: مربع إنجاز (checklist)، تنبيه كهرماني (warning)، نصيحة زرقاء (tip)، مدة تنفيذ، وسوم «المستندات المطلوبة»، أو عناصر تفاعلية خاصة (حاسبة التكلفة، شبكة الخدمات، روابط أحياء)، وزر CTA مثل «ساعدني في هذا».

3. **استكشف كل ما يقدّمه رفيق** (`home.explore.title`) — سطر تعريفي: «كل خدمة ودليل على بُعد نقرة واحدة.» (`home.explore.subtitle`). شبكة من 8 بطاقات روابط ثابتة، كل بطاقة بأيقونة وعنوان ووصف وزر «اعرف المزيد» (`common.learnMore`): «حيل إسطنبول» (`/tricks`)، «الإقامة» (`/residency`)، «العقارات» (`/real-estate`)، «السياحة العلاجية» (`/health-tourism`)، «الخريطة» (`/map`)، «الأدلة» (`/hub`)، «ادعُ واربح» (`/referrals`)، «الباقات» (`/pricing`).

4. **الأسئلة الشائعة** (`home.faq.title` = «الأسئلة الشائعة») — قائمة من 6 عناصر طيّ/فتح (`<details>`) تعرض السؤال ويُفتح ليُظهر الجواب: «هل أحتاج تصريح إقامة للعيش في إسطنبول؟»، «كيف أفتح حسابًا بنكيًا كأجنبي؟»، «ما هو الرقم الضريبي (Vergi No) وكيف أحصل عليه؟»، «هل صحيح أن بعض أحياء إسطنبول مغلقة أمام المقيمين الجدد؟»، «هل يمكنني الحصول على الجنسية التركية بشراء عقار؟»، «كيف يعمل الموعد المجاني مع الخبير؟». ثم بطاقة CTA زرقاء في الأسفل بعنوان «ما زال لديك سؤال؟» (`home.faq.ctaTitle`) ونص «اسأل مساعد رفيق الذكي بلغتك — يجيب فورًا ويوصلك بخبير عند الحاجة.» وزر «اسأل المساعد» (`home.faq.ctaButton`) يقود إلى `/premium`.

### Tricks — المسار `/tricks`

الترتيب الفعلي في `src/pages/Tricks.tsx`:

1. **ترويسة الصفحة** — لوحة بصرية لأفق إسطنبول (`IstanbulSkyline`) فوقها العنوان **«حيل إسطنبول»** (`tricks.title`) وسطر تعريفي: «تطبيقات واختصارات يعرفها أهل البلد تسهّل حياتك اليومية في إسطنبول.» (`tricks.subtitle`).

2. **شبكة الحيل** — 8 بطاقات (`TrickCard`) كل منها بأيقونة وعنوان ووسم نوع: «تطبيق» (`tricks.appBadge`) أو «نصيحة» (`tricks.tipBadge`). بطاقات نوع «تطبيق» تحوي زر «حمّل التطبيق» (`tricks.openApp`) يفتح رابطًا خارجيًا. البطاقة الأولى مميَّزة بعرض كامل وخلفية زرقاء. العناوين الفعلية: «e-Devlet — المفتاح الرئيسي»، «e-İkamet (دائرة الهجرة)»، «تطبيق إسطنبول كارت»، «BiTaksi / iTaksi»، «Getir و Yemeksepeti»، «اشترِ eSIM قبل وصولك»، «احصل على رقمك الضريبي أونلاين أولًا»، «لا تركب تاكسي بلا عدّاد».

3. **بطاقة CTA سفلية** — بأيقونة محادثة، عنوان «محتار في أمر معيّن؟» (`tricks.ctaTitle`)، نص «اسأل مساعد رفيق الذكي — يعرف التطبيقات والدوائر والاختصارات.» (`tricks.ctaBody`)، وزر «اسأل المساعد» (`tricks.ctaButton`) يقود إلى `/premium`.

### Hub — المسار `/hub`

الترتيب الفعلي في `src/pages/Hub.tsx` (دالة `Hub`):

1. **ترويسة نصية** — العنوان **«مركز الأدلة»** (`hub.title`) وتحته سطر «أدلة عملية ومحدّثة للحياة في إسطنبول.» (`hub.subtitle`).

2. **شبكة الأدلة** — 6 بطاقات ثابتة (`GUIDE_SLUGS`)، كل بطاقة بأيقونة وعنوان ومقتطف (excerpt) وزر «اقرأ الدليل» (`hub.readMore`) يقود إلى `/hub/:slug`. العناوين والمقتطفات الفعلية:
   - «إسطنبول كارت: بطاقة المواصلات الوحيدة التي تحتاجها» — «مترو وحافلة وترام وعبّارة — بطاقة واحدة لكل شيء.»
   - «eSIM أم شريحة المطار: لا تدفع زيادة» — «أكشاك المطار تحاسب السياح ضعف السعر. إليك الطريقة الذكية.»
   - «İkamet: تصريح الإقامة من الألف إلى الياء» — «مسار التقديم كاملًا، والمستندات، وفخ الأحياء المغلقة.»
   - «Vergi No: رقمك الضريبي التركي» — «مجاني وسريع ومطلوب لكل شيء تقريبًا.»
   - «فتح حساب بنكي تركي» — «أي البنوك تقبل الأجانب وماذا تطلب.»
   - «اختيار حيّك في إسطنبول» — «الطابع والأسعار وعامل تصريح الإقامة.»

### HubDetail — المسار `/hub/:slug`

الترتيب الفعلي في `src/pages/Hub.tsx` (دالة `HubDetail`):

- إذا كان الـ slug غير صالح: تُعرَض رسالة خطأ «حدث خطأ ما. حاول مرة أخرى.» (`common.error`) مع زر «الأدلة» (`nav.hub`) للعودة.
- إذا كان صالحًا: رابط رجوع علوي «الأدلة» (`nav.hub`)، ثم بطاقة كبيرة تحوي أيقونة الدليل، العنوان الكامل للدليل (`hub.guides.<slug>.title`)، النص الكامل المطوّل (`hub.guides.<slug>.body` — مثل الشرح الكامل لإسطنبول كارت أو مسار İkamet)، وزر سفلي «ساعدني في هذا» (`common.helpMe`) يقود إلى `/premium`.

### Smart — المسار `/smart`

الترتيب الفعلي في `src/pages/Smart.tsx` (لوحة تشخيص مشتقّة من ملف المستخدم):

1. **ترويسة نصية** — العنوان **«الاستشارة الذكية»** (`smart.title`) وتحته «تشخيص مباشر لوضعك الحالي وما يجب فعله تاليًا.» (`smart.subtitle`).

2. **بطاقة «اكتمال التجهيز»** (`smart.completeness`) — دائرة تقدّم (SVG) تعرض نسبة مئوية محسوبة من 4 عناصر يملكها المستخدم، وبجانبها قائمة بالعناصر الأربعة مع علامة صح/دائرة فارغة: «رقم هاتف تركي»، «الرقم الضريبي (Vergi No)»، «تصريح الإقامة (İkamet)»، «حساب بنكي» (مفاتيح `onboarding.q3.*`).

3. **بطاقة «العناصر الناقصة»** (`smart.missing`) — إن لم ينقص شيء تظهر رسالة خضراء «لا ينقصك شيء — تجهيزك مكتمل!» (`smart.noMissing`)؛ وإلا قائمة تنبيهات كهرمانية بالعناصر الناقصة. أسفلها زر «ناقش الأمر مع المساعد الذكي» (`smart.askAi`) يقود إلى `/premium`.

4. **بطاقة «الخطوات التالية الموصى بها»** (`smart.nextSteps`) — قائمة مرقّمة بأول 4 بطاقات من `blocksFor(profile)`، كل عنصر رابط مرقّم بعنوان البطاقة يقود إلى وجهتها (`block.ctaTo`).

### Residency — المسار `/residency`

الترتيب الفعلي في `src/pages/Residency.tsx`:

1. **ترويسة بصرية** — أفق إسطنبول فوقه العنوان **«تصاريح الإقامة (İkamet)»** (`residency.title`) وسطر «كل أنواع الإقامة مشروحة خطوة بخطوة.» (`residency.subtitle`).

2. **تنبيه كهرماني** — «نحو 25% من أحياء إسطنبول مغلقة حاليًا أمام تسجيلات الإقامة الجديدة. تحقق دائمًا من الحي قبل توقيع عقد الإيجار.» (`residency.warning`).

3. **شبكة أنواع الإقامة** — 4 بطاقات، كل بطاقة برأس مصوّر لبطاقة İkamet (`IkametCard`) ثم أيقونة وعنوان ووصف وزر «ساعدني في هذا» (`common.helpMe`) يقود إلى `/premium`. الأنواع الفعلية:
   - «الإقامة قصيرة الأمد» — «أكثر التصاريح شيوعًا للبداية — عادة لسنة أو سنتين، بأساس السياحة أو تملك عقار أو روابط عمل.»
   - «إقامة العائلة» — «للأزواج وأبناء مواطن تركي أو أجنبي يحمل تصريحًا ساريًا. الوثائق المدنية تحتاج أبوستيل + ترجمة محلفة.»
   - «إقامة الطالب» — «للطلاب المسجلين في الجامعات. خطاب التسجيل الجامعي يغني عن معظم المبررات الأخرى.»
   - «الإقامة طويلة الأمد» — «بعد 8 سنوات من الإقامة القانونية المتواصلة — دائمة وبلا حاجة للتجديد.»

4. **بطاقة «مسار التقديم»** (`residency.steps.title`) — قائمة مرقّمة من 4 خطوات: «احصل على الرقم الضريبي والتأمين الصحي.»، «سجّل عنوانك (عقد إيجار موثق).»، «قدّم الطلب عبر e-İkamet واحجز الموعد.»، «احضر الموعد بمستنداتك؛ وتصلك البطاقة بالبريد.»

### RealEstate — المسار `/real-estate`

الترتيب الفعلي في `src/pages/RealEstate.tsx`:

1. **ترويسة بصرية** — أفق إسطنبول فوقه العنوان **«العقارات في إسطنبول»** (`realEstate.title`) وسطر «عروض مختارة مع فحوص قانونية منجزة عنك.» (`realEstate.subtitle`).

2. **شريط ملاحظة أزرق** — بأيقونة درع: «كل عرض موثّق: سند الملكية (tapu) والديون وحالة التخطيط مفحوصة من فريقنا القانوني.» (`realEstate.note`).

3. **شبكة العروض** — تُحمَّل من الخادم (`/api/listings`)، وأثناء التحميل تظهر 3 بطاقات هيكلية نابضة. كل بطاقة عقار تحوي صورة (مع بديل تدرّج لوني وأيقونة مبنى عند الفشل)، اسم الحي، وسم «مؤهل للجنسية» (`realEstate.citizenshipBadge`) إن انطبق، الغرف والمساحة بـ «م²» (`realEstate.perM2`)، السعر بالدولار، وزر «اطلب التفاصيل» (`realEstate.cta`) — وبعد الطلب يتحول إلى «تم إرسال الطلب! سيتواصل معك مستشارنا.» (`realEstate.requested`). الطلب يُنشئ lead؛ والمستخدم غير المسجّل يُحوَّل إلى `/auth`.

### HealthTourism — المسار `/health-tourism`

الترتيب الفعلي في `src/pages/HealthTourism.tsx`:

1. **ترويسة نصية** — العنوان **«السياحة العلاجية»** (`health.title`) وتحته «علاج بمستوى عالمي بأسعار إسطنبول.» (`health.subtitle`).

2. **شبكة الخدمات** — 3 بطاقات ثابتة، كل بطاقة بأيقونة وعنوان ووصف وزر «احصل على عرض سعر مجاني» (`health.cta`) — وبعد الطلب يتحول إلى «تم إرسال طلب العرض! سنرد خلال 24 ساعة.» (`health.requested`). الطلب يُنشئ lead، والمستخدم غير المسجّل يُحوَّل إلى `/auth`. الخدمات الفعلية:
   - «زراعة الشعر (DHI / FUE)» — «عيادات معتمدة من JCI، تسعير شفاف للبصيلات، وباقات تشمل الفندق والمواصلات.»
   - «علاج الأسنان» — «زراعات وفينير وتجميل كامل للابتسامة — عادة أقل بنسبة 50–70% من أسعار أوروبا.»
   - «فحص شامل» — «فحص متكامل في مستشفيات راقية بمواعيد خلال الأسبوع نفسه.»

### MapPage — المسار `/map`

الصفحة ملفوفة بـ `RequireAuth` (تتطلب تسجيل دخول). الترتيب الفعلي في `src/pages/MapPage.tsx`:

**في حالة القفل** (الخادم يرد بـ 402/401 لأن بيانات الأماكن متاحة لباقتي برو/إيليت فقط): يظهر العنوان «خريطة خدمات إسطنبول» (`map.title`) وسطر «أماكن موثوقة، مصفّاة حسب احتياجك.» (`map.subtitle`)، ثم خريطة حقيقية مموّهة (blur) فوقها بطاقة قفل بأيقونة قفل وعنوان «رقِّ باقتك لفتح الخريطة» (`map.locked.title`)، نص «خريطة الخدمات التفاعلية بالمواقع الموثوقة متاحة لباقتي برو وإيليت.» (`map.locked.body`)، وزر «افتحها مع برو» (`map.locked.cta`) يقود إلى `/pricing`.

**في الحالة المفتوحة**، الأقسام بالترتيب:

1. **ترويسة بصرية** — أفق إسطنبول فوقه العنوان «خريطة خدمات إسطنبول» وسطره التعريفي نفسه أعلاه.

2. **شريط البحث الذكي** — حقل إدخال بعبارة «اسأل الذكاء الاصطناعي أين تذهب...» (`map.ai`) وزر «ابحث» (`map.aiGo`)؛ يحلّل النص ويختار الفئة المطابقة محليًا.

3. **أزرار الفئات** — صف وسوم للتصفية: «الكل»، «مطاعم»، «فنادق»، «كاتب عدل»، «مستشفيات»، «دوائر حكومية»، «تسوق» (`map.categories.*`).

4. **الخريطة التفاعلية** — خريطة Leaflet/OpenStreetMap بعلامات للأماكن، وكل علامة لها Popup يعرض الاسم وفئته.

5. **دليل الخدمات** (`map.directory.title` = «دليل الخدمات») — بجانب العنوان عدّاد «{{count}} مكانًا» (`map.directory.count`). إن كانت الفئة فارغة تظهر رسالة «لا أماكن في هذه الفئة بعد.» (`map.directory.empty`)؛ وإلا شبكة بطاقات كل منها بأيقونة الفئة، اسم المكان، عنوانه إن وُجد، وسم الفئة، وزر «الاتجاهات» (`map.directory.directions`) يفتح الموقع على OpenStreetMap.

### Premium — المسار `/premium` (وكذلك `/chat` كـ alias)

الصفحة ملفوفة بـ `RequireAuth`. هي واجهة المحادثة (`ChatUI`) في `src/pages/Premium.tsx`. الأقسام بالترتيب:

1. **الترويسة** — شعار التطبيق، العنوان **«مركز الاستشارات الذكية»** (`chat.title`) وتحته «اسأل عن أي شيء: الإقامة، العقارات، الصحة أو الاستقرار.» (`chat.subtitle`). للمستخدم غير المشترك (غير برو/إيليت) يظهر وسم «متبقٍ {{count}} رسائل مجانية» (`chat.previewLeft`).

2. **نافذة المحادثة** — تبدأ برسالة ترحيب من المساعد: «مرحبًا! أنا رفيق، مساعدك في إسطنبول. أخبرني بما تعمل عليه — إقامة، بنوك، سكن، صحة — وسأرشدك خطوة بخطوة.» (`chat.greeting`). تتدفّق ردود المساعد (streaming) حيًّا. عند رصد حاجة لخبير بشري يظهر عرض حجز يتضمن ملخص المشكلة ونص «يبدو أن حالتك تستفيد كثيرًا من خبير بشري...» (`chat.bookOffer`) وزر «احجز موعدًا مجانيًا» (`chat.bookCta`) يفتح `BookingModal`. عند الخطأ يظهر تنبيه «المساعد غير متاح حاليًا.» (`chat.error`) وزر «إعادة المحاولة» (`chat.retry`). عند نفاد الرسائل المجانية تظهر بطاقة بوّابة دفع بعنوان «المساعد الذكي ميزة لباقة برو» (`chat.gate.title`)، نص «رقِّ إلى برو أو إيليت للدردشة مع رفيق الذكي بلا حدود وحجز مواعيد مجانية مع الخبراء.» (`chat.gate.body`)، وزر «عرض الباقات» (`chat.gate.cta`) يقود إلى `/pricing`.

3. **شريط الإدخال** — حقل بعبارة «صف وضعك...» (`chat.placeholder`) وزر «إرسال» (`common.send`)، وتحته تنويه «رفيق الذكي يقدم إرشادًا عامًا وليس استشارة قانونية.» (`chat.disclaimer`).

### Pricing — المسار `/pricing`

الترتيب الفعلي في `src/pages/Pricing.tsx`:

1. **ترويسة نصية** — العنوان **«اختر باقتك»** (`pricing.title`) وتحته «رقِّ باقتك في أي وقت. وألغِ في أي وقت.» (`pricing.subtitle`).

2. **لوحة الاشتراك النشط** (تظهر فقط لمن لديه اشتراك فعّال) — بعنوان «اشتراكك» (`pricing.active.title`)، تعرض الباقة الحالية، تاريخ التجديد «يتجدد في {{date}}» (`pricing.active.renews`)، ووسم «متبقٍ {{count}} يومًا» (`pricing.active.daysRemaining`). يظهر زر «إلغاء الاشتراك» (`pricing.active.cancelBtn`)، أو ملاحظة كهرمانية «تم إلغاء اشتراكك. سيبقى فعالًا حتى نهاية الفترة المدفوعة.» (`pricing.cancelFlow.done`) إن كان ملغى.

3. **مفتاح الفوترة** — زرّان «شهري» (`common.monthly`) و«سنوي» (`common.annual`)؛ وعند اختيار السنوي يظهر سطر أخضر «شهران مجانًا مع الفوترة السنوية» (`pricing.annualNote`).

4. **شبكة الباقات** — 3 بطاقات: باقة «برو» موسومة بوسم «موصى به» (`common.recommended`). كل بطاقة بها اسم الباقة ووصفها والسعر بالليرة (مع «/ شهريًا» وسطر السعر السنوي عند الفوترة السنوية) وقائمة مزايا بعلامات صح، وزر «اختر {{plan}}» (`pricing.choose`) يقود إلى `/checkout`، أو «باقتك الحالية» (`pricing.current`) معطّلًا للباقة الحالية. الباقات الفعلية:
   - **«لايت»** — «الأساسيات للانطلاق». المزايا: «كل الأدلة بأربع لغات»، «قائمة مهام مخصصة»، «تذكيرات التجديد»، «دعم عبر البريد».
   - **«برو»** — «القوة الكاملة: مساعد ذكي + خريطة». المزايا: «كل ما في لايت»، «استشارات ذكاء اصطناعي بلا حدود»، «خريطة الخدمات التفاعلية»، «مواعيد مجانية مع الخبراء»، «خزانة المستندات».
   - **«إيليت»** — «دعم انتقال راقٍ ومتكامل». المزايا: «كل ما في برو»، «مستشار شخصي مخصص»، «استقبال VIP من المطار»، «ننجز معاملاتك الورقية عنك»، «أولوية في الحجوزات».

5. **نافذة تدفّق الإلغاء** (modal، تظهر عند الضغط على «إلغاء الاشتراك») — بعنوان «يحزننا رحيلك» (`pricing.cancelFlow.title`)، سؤال «لماذا تلغي اشتراكك؟» (`pricing.cancelFlow.reasonLabel`) مع خيارات راديو: «السعر مرتفع»، «لا أستخدمه بما يكفي»، «تنقصه ميزات أحتاجها»، «غادرت تركيا»، «سبب آخر». ثم حقل تعليق «ما الذي يمكننا تحسينه؟» (`pricing.cancelFlow.comments`)، وزرّا «الاحتفاظ بباقتي» (`pricing.cancelFlow.keep`) و«تأكيد الإلغاء» (`pricing.cancelFlow.confirm`).

### Checkout (إتمام الشراء) — `/checkout`

محمية بـ `RequireAuth` (تتطلب تسجيل الدخول). العنوان: **«إتمام الشراء»** (`checkout.title`). تقرأ الباقة ونوع الفوترة من باراميترات الـ URL (`?plan=&billing=`).

الأقسام بالترتيب:

1. **ملخص الطلب** (`checkout.summary`): بطاقة فيها صفوف «الباقة» / «الفوترة» / «الإجمالي». الإجمالي يُحسب عميلًا: شهري = سعر الباقة؛ سنوي = السعر × 10 (يعادل دفع 10 أشهر) مع سطر تقديري **«≈ {{price}} ليرة / شهريًا»** والصيغة السنوية **«{{price}} ليرة / سنويًا»**.
2. **تبويبات الدفع** (تظهر في حالة idle): ثلاثة تبويبات `checkout.tabs` — **«بطاقة»** / **«حوالة بنكية»** / **«عملات رقمية»**:
   - تبويب البطاقة: السطر التعريفي **«تتم معالجة الدفع بالبطاقة بأمان عبر Whop. سيتم تحويلك لإتمام الدفع.»** وزر **«ادفع بالبطاقة»**. عمليًا يُنشئ جلسة دفع ويُحوّل عبر `window.location.href` إلى صفحة البوابة المستضافة (في وضع التطوير: dev gateway simulator + webhook موقّع HMAC).
   - تبويب الحوالة البنكية: عنوان **«حوّل إلى حسابنا بالليرة»**، حقلا IBAN و«صاحب الحساب» قابلان للنسخ (تأتي قيمهما من إعداد الخادم عبر `checkout.config()`، لا شيء ثابت في الواجهة)، زر رفع إيصال (`accept` للصور وPDF) بنص **«ارفع إيصال التحويل»**، ثم زر **«نعم، لقد دفعت»**.
   - تبويب العملات الرقمية: عنوان **«ادفع بـ USDT / USDC»**، حقل قابل للنسخ يجمع «الشبكة» وعنوان المحفظة، ثم زر **«نعم، لقد دفعت»**.
3. **نافذة تأكيد الدفع اليدوي** (Modal): العنوان **«أكّد دفعتك»**، النص **«يتحقق فريقنا من المدفوعات خلال ساعات قليلة. تُفعَّل باقتك فور تأكيد الدفع.»**، وزرّا **«أؤكد أنني أرسلت المبلغ»** و«إلغاء».
4. **حالات الصفحة** (تستبدل التبويبات حسب الحالة): النجاح **«باقتك مفعّلة الآن. أهلًا بك معنا!»**؛ التحقق **«جارٍ التحقق من دفعتك...»** (يستطلع حالة الدفع حتى 10 مرات)؛ المعلّق **«تم استلام الدفع — قيد المراجعة. سنفعّل باقتك قريبًا.»**؛ الفشل **«فشل الدفع أو أُلغي. لم يُخصم أي مبلغ.»** مع زر «إعادة المحاولة».

### Auth (المصادقة) — `/auth`

إن كان المستخدم مسجَّلًا تظهر بطاقة مختصرة فيها الشعار، السطر **«مسجَّل الدخول باسم»** + البريد، وزر «تسجيل الخروج». وإلا تظهر بطاقة المصادقة:

- العنوان يتبدّل حسب الوضع: **«أهلًا بعودتك»** (دخول) أو **«أنشئ حسابك»** (تسجيل)، تحته السطر التعريفي **«سجّل الدخول لمزامنة خطتك ومستنداتك واشتراكك.»**.
- **زر Google**: إذا ضُبط `GOOGLE_CLIENT_ID` يُعرَض زر Google Identity Services الحقيقي؛ وإلا زر تطوير **«المتابعة عبر Google»** (يسجّل بحساب تجريبي).
- فاصل بنص **«أو عبر البريد الإلكتروني»**.
- **النموذج**: حقل «الاسم الكامل» (في وضع التسجيل فقط)، «البريد الإلكتروني»، «كلمة المرور» (حد أدنى 8 أحرف عند التسجيل)، وزر **«تسجيل الدخول»** / **«إنشاء حساب»**.
- رابط تبديل الوضع: **«ليس لديك حساب؟ أنشئ واحدًا»** ↔ **«لديك حساب بالفعل؟ سجّل الدخول»**.
- رسائل الخطأ المترجمة (`auth.errors`): «لا يوجد حساب بهذا البريد الإلكتروني.»، «كلمة المرور غير صحيحة. حاول مرة أخرى.»، «هذا البريد مسجَّل بالفعل — سجّل الدخول بدلًا من ذلك.»، «يجب ألا تقل كلمة المرور عن 8 أحرف.»، «فشل تسجيل الدخول. حاول مرة أخرى.».

### ProfilePage (الملف الشخصي) — `/profile`

محمية بـ `RequireAuth`. العنوان **«ملفي الشخصي»** مع زر «تسجيل الخروج». شبكة من أربع بطاقات + قسم سفلي:

1. **«وضعي»** (`profile.persona`): صفوف «مرحلة الرحلة» و«السبب» و«مع العائلة» (قيمها مشتقة من إجابات الـ onboarding)، وزر **«إعادة الأسئلة التعريفية»** الذي يعيد فتح أسئلة الـ onboarding.
2. **«قائمة التجهيز»** (`profile.checklist`): شريط تقدّم مع نص **«أُنجز {{done}} من {{total}}»**، وقائمة أربعة عناصر قابلة للتبديل بالنقر: **«رقم هاتف تركي»**، **«الرقم الضريبي (Vergi No)»**، **«تصريح الإقامة (İkamet)»**، **«حساب بنكي»** (مصدر النصوص `onboarding.q3`).
3. **«متتبع التجديدات»** (`profile.renewals`): ثلاثة صفوف — **«تصريح الإقامة (İkamet)»**، **«التأمين الصحي»**، **«جواز السفر»** — لكلٍّ منتقي تاريخ. يُحتسب عدد الأيام المتبقية ويُعرض ملوّنًا: «منتهٍ» (أحمر إن مرّ التاريخ)، **«متبقٍ {{count}} يومًا»** (كهرماني دون 30 يومًا، أخضر فوقها)، أو **«لم يُحدد تاريخ»**.
4. **«خزانة المستندات»** (`profile.locker`): قائمة المستندات المرفوعة (اسم + «رُفع في» + زر «تنزيل») أو نص فارغ **«لا مستندات بعد. ارفع نسخ جوازك وعقودك وترجماتك لحفظها بأمان.»**، وزر **«رفع مستند»** (صور وPDF).
5. **«معاملاتي»** (`profile.pipeline`): يجمع حجوزات المستخدم (`bookings.mine`) مع شارة حالة (جديد/مؤكد/منجز/ملغى) وطلبات الخدمة (`leads.mine`) معروضة كـ «عقارات»/«سياحة علاجية» + اسم العنصر؛ أو النص الفارغ **«لا معاملات نشطة. ستظهر الحجوزات والمدفوعات هنا.»**.

### Notifications (الإشعارات) — `/notifications`

إن لم يكن المستخدم مسجَّلًا تظهر بطاقة قفل بالنص **«أنشئ حسابًا مجانيًا لحفظ خطتك ومستنداتك وحجوزاتك.»** وزر «تسجيل الدخول» نحو `/auth`. للمستخدم المسجَّل:

- العنوان **«الإشعارات»**، وزر **«تحديد الكل كمقروء»** (يظهر فقط عند وجود إشعار غير مقروء).
- إن لم توجد إشعارات: بطاقة فارغة بأيقونة صندوق ونص **«لا جديد لديك.»**.
- وإلا: قائمة بطاقات، كل واحدة بأيقونة (جرس، أو مكبّر صوت للإشعارات المخصّصة) + عنوان ونص الإشعار + طابع زمني + نقطة حمراء للإشعار غير المقروء. عناوين/نصوص الإشعارات النظامية معرّفة في `notifications.*` (مثل **«أهلًا بك في رفيق إسطنبول!»**، **«الدفع قيد المراجعة»**، **«تم تفعيل الباقة»**، **«تم تأكيد الموعد»**، **«تم استلام الطلب»**)، أما إشعارات الأخبار العامة فتُعرض بنصّها المخصّص (`customText`).

### Referrals (الإحالات) — `/referrals`

محمية بـ `RequireAuth`. العنوان **«ادعُ واربح»** والسطر التعريفي **«شارك رفيق مع أصدقائك واربح 30% من كل دفعة يسددونها.»**.

1. **بطاقة الرابط والإحصاءات**: عنوان فرعي **«رابط الدعوة الخاص بك»** مع حقل الرابط (`/r/<code>`) وزر «نسخ» / «تم النسخ!». تحته ثلاث بطاقات إحصائية (`referrals.stats`): **«نقرات الرابط»**، **«سجّلوا»**، **«أرباحك»** (بالليرة).
2. **«كيف تعمل»** (`referrals.how`): قائمة مرقّمة بثلاث خطوات — **«شارك رابطك الشخصي مع أصدقاء ينتقلون إلى تركيا.»**، **«يسجّلون ويشتركون في أي باقة.»**، **«تحصل على عمولة 30% من كل دفعة، مدى الحياة.»**.

### Admin (لوحة الإدارة) — `/admin`

محمية بـ `RequireAdmin`. العنوان **«لوحة الإدارة»** مع زر يقود إلى **«الحجوزات»** (`/admin/bookings`). الأقسام بالترتيب الفعلي:

1. **بطاقات إحصائية سريعة** (`admin.users`): **«إجمالي المستخدمين»**، **«المشتركون المدفوعون»**، **«الحجوزات»**، **«الطلبات»**.
2. **«أسعار العملات»** (`admin.rates`): السطر التعريفي **«حدّد سعر USD/TRY و EUR/TRY اليومي الظاهر في الشريط الأسود العلوي. اتركها على المصدر المباشر، أو استبدلها بالسعر الرسمي اليومي.»**؛ حقلا USD/TRY و EUR/TRY، زر **«حفظ الأسعار»** (يصبح «تم الحفظ» مؤقتًا)، وزر **«استخدام المصدر المباشر»** (يمسح التجاوز). سطر حالة سفلي: إمّا **«تم ضبط السعر الرسمي — حُدّث {{date}}»** أو **«يُستخدم حاليًا المصدر المباشر.»**.
3. **«المستخدمون»** (`admin.users`): جدول بأعمدة المستخدم/البريد، تاريخ الانضمام، النقرات، التسجيلات، الحجوزات، الطلبات، الأرباح (بالليرة)، والباقة. عمود الباقة قائمة منسدلة لتغيير الباقة بين «مجاني / لايت / برو / إيليت». المشرفون موسومون بأيقونة درع.
4. **«التحقق من المدفوعات»** (`admin.payments`): قائمة المدفوعات المعلّقة، لكل صف البريد + شارة «الباقة/الفوترة» + «الطريقة» والمبلغ + رابط **«عرض الإيصال»** (إن وُجد إيصال) + زرّا **«تأكيد»** و**«رفض»**؛ أو النص الفارغ **«لا مدفوعات معلقة.»**.
5. **«طلبات الخدمات»** (`admin.leads`): قائمة كل طلبات الخدمة، لكل صف شارة النوع (عقارات/سياحة علاجية) + اسم العنصر + بريد المستخدم + طابع زمني؛ أو **«لا طلبات بعد.»**.
6. **«عروض العقارات»** (`ListingsManager` / `admin.listings`): زر **«إضافة عقار»** يفتح نافذة تحرير بحقول «الحي» و«الغرف» و«المساحة (م²)» و«السعر (دولار)» و«رابط الصورة» وخانة «مؤهل للجنسية»؛ وكل صف في القائمة يعرض الحي والتفاصيل وشارة «مؤهل للجنسية» مع زرّي تعديل وحذف (تأكيد الحذف **«حذف هذا العقار؟»**). رسالة فارغة **«لا عقارات بعد.»**.
7. **«خدمات الخريطة»** (`PlacesManager` / `admin.places`): زر **«إضافة مكان»** يفتح نافذة بحقول «الاسم» و«الفئة» (مطاعم/فنادق/كاتب عدل/مستشفيات/دوائر حكومية/تسوق) و«خط العرض» و«خط الطول» و«العنوان»؛ كل صف يعرض شارة الفئة والاسم والعنوان مع زرّي تعديل وحذف (**«حذف هذا المكان؟»**). رسالة فارغة **«لا أماكن بعد.»**.
8. **«إشعارات الأخبار العامة»** (`admin.news`): حقل نشر بالـ placeholder **«انشر تنبيهًا لكل المستخدمين (مثلًا: قواعد إقامة جديدة)...»** وزر **«نشر»**، تحته قائمة الإشعارات المنشورة سابقًا أو **«لا إشعارات منشورة.»**.

### AdminBookings (حجوزات الخبراء) — `/admin/bookings`

محمية بـ `RequireAdmin`. العنوان **«حجوزات مواعيد الخبراء»**. إن لم توجد حجوزات: بطاقة فارغة بنص **«لا حجوزات بعد.»**. وإلا قائمة بطاقات، كل بطاقة (`Row`) تحتوي:

- بريد المستخدم، ملخص المشكلة، وسطر يجمع **«التاريخ والوقت المفضلان»** و**«اللغة»** (تُعرض باسمها الأصلي).
- قائمة منسدلة لتغيير حالة الحجز بين **«جديد»** / **«مؤكد»** / **«منجز»** / **«ملغى»** (`adminBookings.statuses`).
- زر **«عرض المحادثة»** / **«إخفاء المحادثة»** يطوي/يفتح نص محادثة الذكاء الاصطناعي (فقاعات المستخدم/المساعد).
- حقل ملاحظة داخلية بالـ placeholder **«ملاحظة داخلية...»** وزر **«حفظ الملاحظة»**.

### Legal (الصفحات القانونية) — `/terms`, `/privacy`, `/refund`

مكوّن واحد (`Legal`) يستقبل `doc` ويعرض بطاقة بعنوان ونص واحد طويل لكل وثيقة:

- **`/terms` — «شروط الخدمة»**: «يقدم رفيق إسطنبول إرشادًا معلوماتيًا وخدمات مساندة للأجانب في تركيا. لسنا مكتب محاماة؛ والإرشاد لا يُعد استشارة قانونية. تتجدد الاشتراكات تلقائيًا حتى الإلغاء. إساءة استخدام المنصة أو إعادة بيع المحتوى أو ادعاءات الدفع الاحتيالية تؤدي إلى إنهاء الحساب. يحكم هذه الشروط القانون التركي.»
- **`/privacy` — «سياسة الخصوصية (KVKK)»**: «نعالج البيانات الشخصية وفق القانون التركي رقم 6698 (KVKK). نجمع ما نحتاجه فقط: إجابات ملفك، بيانات حسابك، المستندات التي تختار حفظها، ونصوص الاستشارات المستخدمة لتحضير مواعيدك. تُخزَّن البيانات بأمان، ولا تُباع أبدًا، وتُحذف عند الطلب. يمكنك ممارسة حقوقك بموجب KVKK (الاطلاع والتصحيح والحذف) بالتواصل معنا في أي وقت.»
- **`/refund` — «سياسة الاسترداد»**: «يمكن إلغاء الباقات الشهرية في أي وقت وتبقى فعالة حتى نهاية فترة الفوترة. الباقات السنوية قابلة للاسترداد جزئيًا خلال أول 14 يومًا. مدفوعات الخدمات المنجزة (ترجمات أو مواعيد قُدمت بالفعل) غير قابلة للاسترداد. تُعاد مدفوعات الحوالات البنكية والعملات الرقمية إلى الحساب/المحفظة المرسلة.»

### NotFound (404) — `*`

صفحة 404 حقيقية تُعرض لأي مسار غير معرّف. بطاقة فيها الشعار، الرقم **«404»**، العنوان **«الصفحة غير موجودة»**، السطر **«الصفحة التي تبحث عنها غير موجودة أو نُقلت.»**، وزر **«العودة إلى الرئيسية»** نحو `/`.

---

## 3. الميزات التفاعلية

لديّ كل ما أحتاجه. أكتب القسم الآن.

---

### اختيار اللغة (LanguageSelector)

شاشة منبثقة بملء الصفحة تظهر **قبل أي شيء آخر** عند أول تحميل للتطبيق. شرط ظهورها في `AppContext.tsx` هو الحالة `langSelected`، التي تُهيَّأ من `localStorage`:

```ts
const [langSelected, setLangSelectedState] = useState(
  () => localStorage.getItem('rafiq_lang_selected') === 'true',
);
```

أي أن الشاشة تظهر طالما المفتاح `rafiq_lang_selected` غير مضبوط على `'true'` في `localStorage`.

- العنوان الظاهر للمستخدم: «أهلًا بك في رفيق إسطنبول»، وتحته «اختر لغتك للمتابعة» (`t('langSelect.title')` و`t('langSelect.subtitle')`).
- تُعرَض اللغات في شبكة من زرّين في كل صف (`grid-cols-2`) من القائمة `LANGS`، وكل زر يحمل اسم لغته الأصلي (`l.native`) واتجاهه الصحيح (`dir={l.dir}`).
- عند الضغط على لغة تُستدعى `pick(code)`: أولًا `await setLanguage(code)` (تبدّل لغة i18next واتجاه الصفحة)، ثم `setLangSelected(true)`.
- دالة `setLangSelected` في الـ context تكتب في `localStorage`: عند `true` تخزّن `localStorage.setItem('rafiq_lang_selected', 'true')` (وعند `false` تحذفه). بهذا لا تظهر بوّابة اللغة مرة أخرى في الزيارات التالية على نفس المتصفّح.

ملاحظة مهمة للتمييز: اختيار اللغة يُخزَّن في مفتاح مستقل (`rafiq_lang_selected`) **منفصل تمامًا** عن مفتاح ملف البروفايل/الأونبوردنغ (`rafiq_profile`). فحذف أحدهما لا يؤثّر في الآخر.

### الأونبوردنغ (OnboardingModal)

يظهر الـ OnboardingModal بعد اختيار اللغة، وشرطه في الـ context هو:

```ts
const onboarded = profile.path !== null;
```

أي أن المودال يظهر طالما لم يُحدَّد مسار (`profile.path === null`) — وهو حالة `EMPTY_PROFILE`. المودال **لا يحجب التطبيق بشكل صارم**؛ يمكن دائمًا تخطّيه، ويُعاد فتحه لاحقًا عبر زر «تغيير إجاباتي» (`home.editAnswers`) الذي يستدعي `resetOnboarding`.

المودال عبارة عن آلة حالات (state machine) من أربع خطوات: `'q1' | 'q2' | 'q3' | 'q4'`، تبدأ من `'q1'`. هيكله شريط علوي كحلي يحمل الشعار وعنوان السؤال الحالي وزر التخطّي، وأسفله جسم كريمي يعرض بطاقات الخيارات.

**Q1 — المسار (`path`):** العنوان «أين أنت في رحلتك إلى تركيا؟». أربع بطاقات (`JourneyPath`):

| القيمة | العنوان الظاهر | الوصف |
|---|---|---|
| `planning` | أخطط للقدوم | لست في تركيا بعد — أُحضّر للانتقال |
| `arrived` | وصلت حديثًا | في إسطنبول منذ 0–3 أشهر |
| `living` | أعيش هنا | مستقر — تجديدات وخدمات |
| `browsing` | أتصفح فقط | أستكشف الثقافة والاستثمار |

الاختيار يستدعي `pickPath(p)`، التي تخزّن المسار وتفرّع التدفّق:
```ts
setStep(p === 'planning' ? 'q2' : 'q3');
```
أي أن **Q2 تظهر فقط لمسار `planning`**؛ باقي المسارات الثلاثة (`arrived` / `living` / `browsing`) تقفز مباشرة إلى Q3.

**Q2 — السبب (`reason`)، لمسار `planning` فقط:** العنوان «لماذا ستأتي؟». خمس بطاقات (`Reason`):

| القيمة | العنوان الظاهر | الوصف |
|---|---|---|
| `tourism` | سياحة | عطلة أو رحلة قصيرة |
| `visiting` | زيارة | العائلة أو الأصدقاء |
| `live` | الانتقال للعيش | انتقال طويل الأمد |
| `work` | العمل | وظيفة أو عمل تجاري |
| `study` | الدراسة | جامعة أو معهد لغة |

الاختيار يستدعي `pickReason(r)` التي تخزّن السبب وتنتقل دائمًا إلى Q3. للمسارات غير `planning` يبقى `reason = null`.

**Q3 — «ما الذي تملكه بالفعل؟»:** تظهر لكل المسارات. عنوان فرعي «اختر كل ما تملكه حاليًا». هذا السؤال **متعدد الاختيار** (checkboxes)، يخزّن في كائن `has`:

| المفتاح | العنوان الظاهر | الوصف |
|---|---|---|
| `turkishPhone` | رقم هاتف تركي | شريحة محلية أو eSIM |
| `taxNumber` | الرقم الضريبي (Vergi No) | رقمك الضريبي التركي |
| `residencePermit` | تصريح الإقامة (İkamet) | بطاقة إقامة سارية |
| `bankAccount` | حساب بنكي | حساب في بنك تركي |

كل بطاقة لها دور `role="checkbox"` مع `aria-checked` ودعم لوحة المفاتيح (Enter/Space)، والضغط يبدّل قيمة المفتاح في `has`. زر «متابعة» (`q3.continue`) ينتقل إلى Q4. هذه الإجابات تتغذّى مباشرة في منطق الكتل الشرطية (إخفاء كتل المهام لما هو مملوك بالفعل).

**Q4 — العائلة (`family`):** العنوان «هل ستأتي مع العائلة أو الأطفال؟». بطاقتان:

| القيمة | العنوان الظاهر | الوصف |
|---|---|---|
| `yes` | نعم، مع العائلة | المدارس وإقامة العائلة والتأمين |
| `no` | لا، بمفردي | خطة فردية |

اختيار أيٍّ منهما يستدعي `finish(family)`.

**بناء البروفايل وتخزينه:** الإنهاء يجمع كل الإجابات في كائن واحد ويمرّره إلى `updateProfile`:
```ts
const finish = (family: 'yes' | 'no') => {
  updateProfile({ path, reason, has, family });
};
```
في `updateProfile` (داخل `AppContext.tsx`) يُدمج الـ patch فوق البروفايل السابق (مع دمج عميق لـ `has` و`completed` و`renewals`)، ثم:
- يُكتب كامل البروفايل إلى `localStorage` تحت المفتاح `rafiq_profile`.
- إذا كان المستخدم مسجّل الدخول (`user`) يُرسَل أيضًا إلى الخادم عبر `profileApi.save(next)` (تخزين سحابي يتبع الحساب عبر الأجهزة). عند تسجيل الدخول لاحقًا تكون أسبقية النسخة الخادمية إن كانت تحوي `path`، وإلا تُرفَع الإجابات المحلية للخادم.

بمجرد أن يصبح `profile.path !== null` تصير `onboarded = true` ويختفي المودال.

**زر «تخطَّ الآن» (`onboarding.skip`):** موجود في الشريط العلوي، وكذلك إغلاق المودال (`onClose`) يستدعي نفس الدالة `skip`:
```ts
const skip = () => {
  updateProfile({ path: 'browsing', reason: null, has, family: 'no' });
};
```
أي أن التخطّي ليس خروجًا فارغًا، بل يُسند المسار `browsing` افتراضيًا (مع `family: 'no'` والحفاظ على أي خانات `has` كان المستخدم قد أشّرها قبل التخطّي). بهذا يهبط المستخدم على تجربة «التصفّح»، ويمكنه دائمًا إعادة الإجابة لاحقًا من «تغيير إجاباتي».

### نظام الكتل الشرطية (registry.ts)

تُعرَّف كل بطاقة في الخطة المخصّصة ككائن `Block` ضمن المصفوفة `BLOCKS`. حقول الكتلة:

- `id` (string): المعرّف الفريد؛ يُستخدم لجلب نصوص الترجمة (`blocks.<id>.title/body/...`) وكذلك لتتبّع الإنجاز في `profile.completed[id]`.
- `icon` (`IconName`): أيقونة من مجموعة `AppIcon`.
- `priority` (number): الأولوية الثابتة (الأصغر يظهر أعلى).
- `priorityFor?` (دالة `(p) => number`): تجاوز ديناميكي للأولوية حسب البروفايل (قواعد إعادة الترتيب)؛ إن وُجدت تُستخدم بدل `priority`.
- `showIf` (دالة `(p) => boolean`): شرط الإظهار بناءً على البروفايل.
- `kind` (`'info' | 'checklist' | 'col' | 'services' | 'nudge'`): يحدّد شكل البطاقة وما تعرضه (مثلًا `col` تركّب الحاسبة، `services` تركّب شبكة الخدمات، `checklist` تضيف صندوق إنجاز).
- `hasKey?`: أيُّ خانة من `profile.has` يكملها هذا البند (للمهام)؛ عند وجودها تُستخدم حالة `has` بدل `completed` لتعليم البند منجزًا.
- `hasDuration?` / `hasDocs?` / `hasWarning?` / `hasTip?`: أعلام لعرض المدّة، والمستندات المطلوبة (`blocks.<id>.docs` كمصفوفة)، وملاحظة تحذير كهرمانية، وصندوق نصيحة.
- `ctaTo` (string): مسار زر الـ CTA الداخلي.

**دالة `blocksFor(profile)`** هي قلب النظام: ترشّح ثم ترتّب.
```ts
return BLOCKS.filter((b) => b.showIf(profile)).sort((a, b) => {
  const pa = a.priorityFor ? a.priorityFor(profile) : a.priority;
  const pb = b.priorityFor ? b.priorityFor(profile) : b.priority;
  return pa - pb;
});
```
أي: أبقِ فقط الكتل التي يُرجِع `showIf` لها `true`، ثم رتّبها تصاعديًا بالأولوية الفعّالة (الديناميكية إن وُجدت، وإلا الثابتة).

**أمثلة فعلية لمنطق الإظهار حسب المسار:**

- مسار `planning`: تظهر `preArrival` («قبل السفر: التأشيرة والمستندات») و`arrivalPackage` («باقة الوصول: أول 48 ساعة») دائمًا. ثم كتلة واحدة تتفرّع حسب `reason`:
  - `tourismGuide` («أساسيات السائح») إذا `reason === 'tourism' || 'visiting'`.
  - `settlementStack` («حزمة الاستقرار: الإقامة والبنك والضريبة») إذا `reason === 'live' || 'work'`.
  - `studyBlock` («الدراسة في تركيا») إذا `reason === 'study'`.
- مسار `arrived` (قائمة مهام الـ14 يومًا، تختفي البنود المملوكة مسبقًا):
  - `simBlock` إذا `!has.turkishPhone`، `taxNumberBlock` إذا `!has.taxNumber`، `bankBlock` إذا `!has.bankAccount`، `residenceBlock` إذا `!has.residencePermit`.
  - `transportBlock` («إسطنبول كارت والتنقل») يظهر دائمًا لهذا المسار (لا يرتبط بأي `hasKey`).
- مسار `living`: تظهر مجموعة ثابتة — `renewalTracker`, `driverLicense`, `healthInsurance`, `realEstateUpgrade`, `companyFormation`, `translators` — كلها بشرط `p.path === 'living'`.
- مسار `browsing`: `costOfLiving` (الحاسبة)، `cityGuide`، `serviceOverview`، `relocateNudge` — كلها بشرط `p.path === 'browsing'`.
- كتل العائلة (مستقلة عن المسار): `schoolsBlock` («مدارس لأطفالك»)، `familyResidence` («إقامة العائلة»)، `familyInsurance` («تأمين صحي عائلي») — كلها بشرط `p.family === 'yes'`، فتنضمّ إلى أي مسار كان عندما يجيب المستخدم بنعم في Q4.

**قواعد إعادة الترتيب (priorityFor):** كتلتان فقط تستخدمان أولوية ديناميكية:

- `residenceBlock`: أولوية ثابتة `5`، لكن `priorityFor: (p) => (!p.has.residencePermit ? 0 : 5)` — أي تطفو إلى أعلى القائمة (أولوية `0`) ما دام تصريح الإقامة مفقودًا، باعتبارها أهم مهمة. (عمليًا الكتلة لا تظهر أصلًا إلا عند فقدان التصريح، فهي تظهر دومًا بأولوية `0`.)
- `renewalTracker`: أولوية ثابتة `10`، و`priorityFor: (p) => (residenceExpired(p) ? 0 : 10)`. الدالة المساعدة `residenceExpired` تتحقق من `p.renewals.residence` وأن تاريخه قبل الآن. فإذا كان تصريح الإقامة منتهيًا تُدفَع الكتلة إلى رأس خطة مسار `living`. ويرافق ذلك في `BlockCard` ملاحظة هرمانية «تصريح إقامتك مسجَّل كمنتهٍ — ابدأ التجديد فورًا لتجنب الغرامات» (`blocks.renewalTracker.expiredNote`).

### الحاسبة التفاعلية (CostOfLiving)

تُركَّب داخل `BlockCard` عندما تكون `block.kind === 'col'` (كتلة `costOfLiving`، مسار `browsing` فقط). عنوانها «حاسبة تكلفة المعيشة» ووصفها «قدّر ميزانيتك الشهرية في إسطنبول حسب الحي ونمط الحياة».

**المدخلات** ثلاث قوائم منسدلة:

1. **الحي** (`district`): «مركزي (قاضي كوي، بشيكتاش)» = `central`، أو «ضواحٍ (إسنيورت، بيليك دوزو)» = `suburban`.
2. **نمط الحياة** (`lifestyle`): «اقتصادي» = `budget`، «مريح» = `comfort` (الافتراضي)، «فاخر» = `premium`.
3. **الأسرة** (`household`): «فرد» = `single` (الافتراضي)، «زوجان» = `couple`، «عائلة (2 + 2)» = `family`.

**البنود المحسوبة تلقائيًا** — كل القيم مشتقّة من المدخلات الثلاثة عبر معاملات، ولا يُدخلها المستخدم يدويًا. المعاملات:
- مضاعِف نمط الحياة `life`: `0.7` اقتصادي، `1` مريح، `1.8` فاخر (ومعامل صحّي منفصل `healthLife`: `0.8` / `1` / `1.6`).
- عدد الأشخاص `persons`: `1` / `2` / `4`، ومعاملات تحجيم لكل بند: إيجار (`1` / `1.25` / `1.6`)، طعام (`1` / `1.8` / `3`)، فواتير (`1` / `1.2` / `1.5`)، ترفيه (`1` / `1.6` / `2.2`).

الصيغ الفعلية (مع تقريب كل قيمة لأقرب 50 ليرة عبر `r2`):

| البند | العنوان الظاهر | الحساب |
|---|---|---|
| `rent` | الإيجار | `(central ? 28000 : 15000) × life × rentFactor` |
| `food` | الطعام والمشتريات | `6000 × life × foodFactor` |
| `transport` | المواصلات | `(central ? 1100 : 1750) × persons` |
| `utilities` | الفواتير والإنترنت | `3000 × life × utilFactor` |
| `health` | التأمين الصحي | `1200 × persons × healthLife` |
| `leisure` | الترفيه والمطاعم | `3500 × life × leisureFactor` |

ثم **الإجمالي** = مجموع البنود الستة، يُعرَض في شريط كحلي بعنوان «الإجمالي الشهري التقديري» (`total`). كل القيم تُنسَّق بـ `toLocaleString()` ولاحقة «common.tl» وتُعرَض باتجاه `dir="ltr"`. وأسفلها تنبيه «تقدير تلقائي بناءً على أسعار 2026 المعتادة. التكاليف الفعلية تختلف» (`disclaimer`).

**كيف تتغيّر القيم:** الحاسبة تحتفظ بحالتها داخليًا عبر `useState` (district/lifestyle/household)، وأي تغيير في قائمة منسدلة يعيد حساب جميع البنود والإجمالي فورًا في إعادة الرسم — مثلًا تبديل الحي من «مركزي» إلى «ضواحٍ» يخفض أساس الإيجار من 28000 إلى 15000 لكنه يرفع أساس المواصلات من 1100 إلى 1750 لكل شخص؛ وتبديل الأسرة إلى «عائلة» يضاعف الطعام (×3) والإيجار (×1.6) والأشخاص (×4 في الصحة والمواصلات). هذه القيم محلية بالكامل ولا تُحفَظ في البروفايل ولا تُرسَل للخادم.

**كتلة دليل الأحياء (CityDistricts):** تُركَّب داخل `BlockCard` خصيصًا عندما `block.id === 'cityGuide'` (كتلة «دليل مدينة إسطنبول»، مسار `browsing`). تعرض تحت عنوان «أحياء مشهورة» (`cityGuide.districtsLabel`) ستّ روابط سريعة بأسماء أحياء حقيقية (أسماء علم، غير مترجَمة): Kadıköy، Beşiktaş، Üsküdar، Şişli، Fatih، Beyoğlu. كل رابط يحمل أيقونة دبوس خريطة ويؤدي إلى `/hub/districts`.

**شبكة الخدمات (ServicesGrid):** تُركَّب عندما `block.kind === 'services'` (كتلة `serviceOverview` «خدماتنا»، مسار `browsing`). شبكة من أربع بطاقات روابط: «الإقامة» → `/residency`، «العقارات» → `/real-estate`، «السياحة العلاجية» → `/health-tourism`، «خريطة الخدمات» → `/map`. هذه الكتلة (مثل كتلة `col`) لا تعرض زر CTA سفليًا لأن البطاقات نفسها هي روابط التنقّل.

**الملفات المرجعية:** `C:/Users/muhmm/Downloads/CLAUDE1/rafiq-istanbul/src/components/LanguageSelector.tsx`، `.../src/components/OnboardingModal.tsx`، `.../src/context/AppContext.tsx`، `.../src/blocks/registry.ts`، `.../src/blocks/BlockCard.tsx`، `.../src/i18n/locales/ar.json`.

### 3-ب-1) محادثة المساعد الذكي (chat-to-booking)

تعيش المحادثة في `/premium` (ومرادفها `/chat`)، مكوّن `Premium.tsx` ملفوف بـ `RequireAuth` فلا يصلها إلا مستخدم مسجّل دخول. العنوان المعروض «مركز الاستشارات الذكية» مع العنوان الفرعي «اسأل عن أي شيء: الإقامة، العقارات، الصحة أو الاستقرار»، وفقاعة ترحيب ثابتة «مرحبًا! أنا رفيق، مساعدك في إسطنبول...» (`chat.greeting`).

__الإرسال والبثّ المتدفّق (SSE streaming).__ عند الإرسال يُستدعى `ai.chat(messages, lang, onDelta)` في `src/lib/api.ts`. الدالة تفتح `POST /api/ai/chat` بجسم `{ messages, lang }`، ثم تقرأ جسم الاستجابة عبر `getReader()` وتفكّك إطارات SSE المفصولة بـ `\n\n`؛ كل إطار `data: {...}`: إن حمل `delta` يُراكَم على النص ويُستدعى `onDelta(reply)` لتحديث الفقاعة حرفًا حرفًا، وإن حمل `done` يُلتقط منه `offerBooking` و`problemSummary` و`remaining`. في الواجهة تُنشأ فقاعة مساعد فارغة `streaming: true` تمتلئ تدريجيًا (مع مؤشر نبضة `animate-pulse`)، ثم تُختَم بالنص الكامل. تاريخ المحادثة يُحفظ محليًا في `localStorage` بمفتاح `rafiq_chat_history_${userId}` (مع استبعاد الرسائل قيد البثّ).

على الخادم في `index.mjs` (المسار `/api/ai/chat`، محميّ بـ `requireAuth`): يأخذ آخر رسالة كنص المستخدم (مقصوص عند 4000 حرف)، يضبط رؤوس `text/event-stream`، يقسّم الردّ إلى مقاطع بطول 40 حرفًا ويرسلها مقطعًا مقطعًا، وبين كل مقطعين تأخير 25ms في وضع الـ fallback و0 مع المفتاح الحقيقي، ثم يرسل إطار `{ done: true, offerBooking, problemSummary, remaining }` ويُنهي.

__الـ system prompt وسياسة التصعيد (`server/ai-policy.mjs`).__ `SYSTEM_PROMPT` يعرّف «رفيق» مساعدًا متعدد اللغات يردّ دائمًا بلغة المستخدم، ويطلب صراحةً إخراج JSON فقط بالشكل `{"reply", "offerBooking", "problemSummary"}`. سياسة الحجز فيه:
- لا يعرض موعدًا بشريًا أبدًا للأسئلة البسيطة التي يجيب عنها بنفسه (مثل «كيف أحصل على إسطنبول كارت»، «ما هو الرقم الضريبي»).
- يعرض موعدًا مجانيًا أونلاين (مكالمة/فيديو) عند __أي__ من: حالة معقّدة/خاصة (رفض، استئناف، مواعيد فائتة، أحياء مغلقة، ملفات جنسية، قضايا محاكم/غرامات، صفقات كبيرة كشراء عقار أو تأسيس شركة)؛ أو ظهور المستخدم «عالقًا» بتكرار سؤاله عن المشكلة نفسها؛ أو طلبه الصريح التحدّث إلى شخص/خبير/محامٍ/مستشار.
- عند العرض: يلخّص المشكلة بلغة المستخدم في جملة-جملتين أولًا، ثم يعرض الموعد المجاني دون فرضه (عرض اختياري، تبقى الدردشة مفتوحة).

إلى جانب قرار النموذج، هناك __شبكة أمان حتمية على الخادم__ في `policyTriggers()` تُطبَّق دائمًا: تَكشف الموضوع عبر `detectTopic` (regex متعدد اللغات لثمانية مواضيع)، وتحسب: `asksHuman` (نمط `HUMAN_PATTERNS` للطلب الصريح)، `isComplex` (نمط `COMPLEX_PATTERNS` لكلمات رفض/استئناف/غرامة/محكمة...)، و`isStuck` = الموضوع غير عام وتكرّر السؤال عنه __3 مرّات أو أكثر__ (`sameTopicCount >= 3`). في الخادم تُدمج النتيجة: `offerBooking = turn.offerBooking || triggers.asksHuman || triggers.isStuck`، وإن غاب الملخّص يُستبدَل بـ `fallbackSummary`. عند ظهور العرض تُضاف فقاعة مساعد ثانية نصّها = الملخّص + «يبدو أن حالتك تستفيد كثيرًا من خبير بشري. يمكنني ترتيب موعد مجاني عبر الإنترنت...» (`chat.bookOffer`)، تحتها زر «احجز موعدًا مجانيًا» (`chat.bookCta`) يفتح `BookingModal`.

__الـ fallback الحتمي بلا مفتاح.__ إذا ضُبط `ANTHROPIC_API_KEY` يستدعي الخادم Anthropic Messages API (نموذج `cfg.anthropicModel`، `max_tokens: 1024`، آخر 20 رسالة، مهلة 30 ثانية) ويحلّل JSON الردّ (وإن فشل يستخدم النص الخام)، وإن فشل النداء يردّ `503 ai_unavailable` فتُظهر الواجهة «المساعد غير متاح حاليًا.» (`chat.error`) مع زر «إعادة المحاولة». أما بدون مفتاح فيستخدم `fallbackRespond()`: يختار ردًّا جاهزًا حسب الموضوع من جدول `REPLIES` بأربع لغات (en/ar/ru/fa) — ردود حقيقية مفصّلة لكل من الإقامة والرقم الضريبي والبنك والسكن والتأمين والشريحة والدراسة والعقارات — ويُحدّد `offerBooking` من `asksHuman || isComplex || isStuck`. فالمساعد يعمل بشكل كامل ومفيد حتى دون أي مفتاح خارجي.

__المعاينة المجانية وبوّابة Pro/Elite.__ في الواجهة، غير المشتركين بـ Pro/Elite تظهر لهم شارة «متبقٍ {{count}} رسائل مجانية» (`chat.previewLeft`) تُحدَّث من `remaining` العائد من الخادم. على الخادم: إذا لم يكن المستخدم pro/elite يُقرأ عدّاد `ai_usage`؛ فإن بلغ `cfg.freeChatMessages` يردّ `402 upgrade_required`، وإلا يزيد العداد ويحسب `remaining`. الواجهة عند 402 تعرض بطاقة بوّابة داخل نافذة الدردشة: «المساعد الذكي ميزة لباقة برو» / «رقِّ إلى برو أو إيليت للدردشة مع رفيق الذكي بلا حدود وحجز مواعيد مجانية مع الخبراء» وزر «عرض الباقات» إلى `/pricing` (`chat.gate.*`). ملاحظة دقيقة: هذه البطاقة الداخلية مرسومة محليًا في `Premium.tsx` وليست مكوّن `UpsellGate`؛ أما `UpsellGate` (في `Gates.tsx`، يتحقق `tier === 'pro' || 'elite'` ويعرض معاينة مموّهة `blur` فوقها بطاقة ترقية) فهو الدرع المستخدم لميزات Pro الأخرى مثل الخريطة. أسفل الصندوق ثابتًا تنويه «رفيق الذكي يقدم إرشادًا عامًا وليس استشارة قانونية» (`chat.disclaimer`).

### 3-ب-2) تدفّق الحجز (`BookingModal`)

يُفتح من زر العرض في الدردشة فقط، ويتلقّى `problemSummary` و`transcript` (نسخة المحادثة الحالية) كـ props. النموذج __أدنى ما يمكن__: المستخدم يملأ ثلاثة حقول فقط:
- التاريخ المفضل (`<input type="date">`) — العنوان «التاريخ المفضل».
- الوقت المفضل (`<input type="time">`) — «الوقت المفضل».
- اللغة المفضلة (`<select>` من قائمة `LANGS` بأسمائها الأصلية) — «اللغة المفضلة»، مهيّأة افتراضيًا على لغة الواجهة الحالية.

ما يُرفق تلقائيًا (ولا يكرّره المستخدم): يُعرض في الأعلى صندوق «ملخص مشكلتك» يحوي `problemSummary` القادم من المساعد، وأسفل النموذج سطر «يُرفَق ملخص مشكلتك ونص المحادثة تلقائيًا — لا حاجة لتكرار أي شيء» (`booking.attachedNote`). عند التأكيد يُستدعى `bookings.create({ problemSummary, transcript, preferredDatetime: \`${date}T${time}\`, preferredLanguage })` — فيُرسَل __ملخّص المشكلة + نص المحادثة كاملًا__ مع الحجز، بينما __هوية المستخدم__ (المعرّف والبريد) تُلحَق على الخادم من الجلسة لا من العميل.

__منع التواريخ الماضية.__ يُمنع على العميل بضبط `min={today}` على حقل التاريخ، وبحساب `isPast = new Date(\`${date}T${time}\`) <= new Date()` الذي يعطّل زر التأكيد ويُظهر تنبيه «اختر تاريخًا ووقتًا في المستقبل» (`booking.errors.past`). والخادم يفرض الأمر نفسه: في `POST /api/bookings` يتحقق من ملخّص غير فارغ، و`transcript` مصفوفة، ولغة ضمن `LANG_CODES`، ويرفض `when <= new Date()` بـ `400 past_datetime`. عند النجاح يُخزَّن في جدول `bookings` (مع قصّ الملخّص عند 500 حرف وتسلسل المحادثة JSON والحالة `new`)، ويُرسَل إشعار للمستخدم «تم طلب الموعد» وآخر مُجمَّع للمشرفين «حجز جديد». تُظهر الواجهة شاشة نجاح «تم طلب الموعد!» / «سيؤكد فريقنا موعدك عبر الإنترنت قريبًا. ستصلك إشعارات هنا» (`booking.success.*`). يظهر الحجز لاحقًا في «معاملاتي» بالبروفايل بحالته، وفي لوحة الأدمين `/admin/bookings`.

### 3-ب-3) Smart: لوحة التشخيص

صفحة `/smart` (`Smart.tsx`) العنوان «الاستشارة الذكية» والفرعي «تشخيص مباشر لوضعك الحالي وما يجب فعله تاليًا». اللوحة تُشتقّ بالكامل من `profile` المحلي (دون نداء خادم)، وتقوم على أربعة عناصر تجهيز من `profile.has`: الهاتف التركي، الرقم الضريبي، تصريح الإقامة، الحساب البنكي.

- __نسبة الاكتمال__ (بطاقة «اكتمال التجهيز»): `owned = عدد العناصر المملوكة`، و`pct = Math.round((owned / 4) * 100)` تُعرض داخل حلقة SVG دائرية (`strokeDasharray={pct} 100`)، بجانبها قائمة العناصر الأربعة كلٌّ بأيقونة `check` (أخضر) إن مُلِك أو `circle` إن لا، وعناوينها من `onboarding.q3.<key>.title`.
- __العناصر الناقصة__ (بطاقة «العناصر الناقصة»): `missing = العناصر غير المملوكة`؛ إن خلت تظهر «لا ينقصك شيء — تجهيزك مكتمل!» (`smart.noMissing`)، وإلا تُسرَد كل عنصر ناقص في سطر تحذيري `amber-note`. تحتها زر «ناقش الأمر مع المساعد الذكي» (`smart.askAi`) إلى `/premium`.
- __الخطوات التالية الموصى بها__ (بطاقة `smart.nextSteps`): `nextBlocks = blocksFor(profile).slice(0, 4)` — أول أربعة بلوكات يحسبها مُحرّك `blocksFor` من البروفايل (المسار/السبب/العائلة/ما يملكه)، كلٌّ يُعرض مُرقّمًا (1..4) برابط إلى `b.ctaTo` وعنوان `blocks.<id>.title`. أي أن الترتيب مخصّص حسب وضع المستخدم وليس قائمة ثابتة.

### 3-ب-4) نماذج طلب الخدمة (leads) — العقارات والصحة

في `RealEstate.tsx` كل بطاقة عقار (من `/api/listings`) تحوي زر «اطلب التفاصيل» (`realEstate.cta`). الضغط — إن لم يكن المستخدم مسجّلًا يُحوَّل إلى `/auth` — يستدعي `leads.create('realestate', \`${l.district} ${l.rooms} · $${l.priceUsd}\`)`، فيُحفظ lead على الخادم (`POST /api/leads`، نوع ضمن `realestate|health`، النص مقصوص عند 200 حرف، حالة `new`) مع إشعار للمستخدم «تم استلام الطلب» وآخر للأدمين «طلب خدمة جديد». بعدها يتحوّل الزر إلى «تم إرسال الطلب! سيتواصل معك مستشارنا» (`realEstate.requested`) ويُعطَّل. وعند إعادة تحميل الصفحة تُجلب `leads.mine()` وتُطابَق بالحي والغرف لإبقاء الأزرار المُرسَلة سابقًا معطّلة (الطلبات مُخزّنة فعلًا لا مجرد حالة واجهة).

في `HealthTourism.tsx` ثلاث خدمات ثابتة (زراعة الشعر، علاج الأسنان، فحص شامل) كلٌّ بزر «احصل على عرض سعر مجاني» (`health.cta`)؛ بالمنطق نفسه `leads.create('health', \`${id} — ${title}\`)`، ثم «تم إرسال طلب العرض! سنرد خلال 24 ساعة» (`health.requested`). كل الـ leads تظهر للمستخدم في «معاملاتي» بالبروفايل، وللأدمين في قسم «طلبات الخدمات». لا يوجد إرسال بريد/واتساب/هاتف — التحويل كله عبر هذا الـ lead المُخزَّن.

### 3-ب-5) خزانة المستندات (document locker)

في `ProfilePage.tsx` بطاقة «خزانة المستندات» (`profile.locker`). زر «رفع مستند» هو `<label>` يخفي `<input type="file">` يقبل `image/png, image/jpeg, image/webp, application/pdf` فقط. الرفع يستدعي `documents.upload(file)` الذي يرسل `FormData` إلى `POST /api/documents` (محميّ بـ `requireAuth`، يحفظ الملف عبر multer ويُدرج صفّ في جدول `documents` مربوطًا بالمستخدم)، ثم تُعاد القائمة. كل مستند يُعرض باسمه وتاريخ رفعه («رُفع في …») وزر «تنزيل» يفتح `documents.fileUrl(id)` = `/api/documents/:id/file` في تبويب جديد — وهذا المسار يتحقق خادميًا أن المالك هو الطالب وإلا `404`. القائمة الفارغة تُظهر «لا مستندات بعد. ارفع نسخ جوازك وعقودك وترجماتك...». (الحذف متاح في الـ API عبر `DELETE /api/documents/:id` لكن لا زرّ له في هذه الشاشة.)

### 3-ب-6) متتبّع التجديدات (renewal tracker)

بطاقة «متتبع التجديدات» في البروفايل تعرض ثلاثة صفوف ثابتة: «تصريح الإقامة (İkamet)»، «التأمين الصحي»، «جواز السفر»، قِيَمها من `profile.renewals`. لكل صف حقل `<input type="date">` يحدّث `profile` محليًا عبر `updateProfile`. الحساب `daysUntil(iso)` يعطي الأيام المتبقية ويُلوّن النص: أحمر إن منتهٍ مع نص «منتهٍ» (`profile.renewals.expired`)، كهرماني إن أقل من 30 يومًا، أخضر خلاف ذلك، مع نص «متبقٍ {{count}} يومًا» (`profile.renewals.daysLeft`) وتاريخ مُنسَّق بلغة الواجهة؛ وإن لم يُحدَّد تاريخ تُعرض «لم يُحدد تاريخ». هذا تتبّع محلي بالكامل (داخل البروفايل) لا يولّد إشعارات خادمية مجدوَلة.

### 3-ب-7) رفع إيصال الدفع (تبويب الحوالة البنكية)

في `Checkout.tsx`، تبويب الحوالة البنكية يعرض الـ IBAN واسم المستفيد عبر حقول نسخ، وزرًا لرفع الإيصال هو `<label>` يخفي `<input type="file">` يقبل صور PNG/JPEG/WebP وPDF؛ بعد الاختيار يتغيّر نصّه إلى «<تم الرفع>: اسم الملف» (`checkout.bank.uploaded`). عند الضغط على «دفعتُ/تأكيد» يُفتح حوار تحقق ثم يُستدعى `checkout.manual(plan, billing, tab, receipt)` الذي يرسل `FormData` (الباقة، دورة الفوترة، الطريقة، والإيصال إن وُجد) إلى `POST /api/payments/manual`. الدفعة تُسجَّل بحالة `pending` بانتظار مراجعة الأدمين (التفعيل ليس تلقائيًا)، ويُتاح للأدمين عرض الإيصال عبر `/api/admin/payments/:id/receipt` ثم «تأكيد» أو «رفض».

### 3-ب-8) محرّرات الأدمين (`AdminManagers`: listings / places + أسعار العملات)

__إدارة عروض العقارات (`ListingsManager`).__ تحت «عروض العقارات» جدول من `listings.adminList()` لكل صفّ الحي والغرف والمساحة والسعر وشارة «مؤهل للجنسية» إن وُجدت، مع زرّي تعديل وحذف. «إضافة عقار»/«تعديل العقار» يفتح `ListingEditor` (داخل `Modal`) بحقول: الحي، الغرف (مثال `2+1`)، المساحة (م²)، السعر (دولار)، رابط الصورة، وخانة «مؤهل للجنسية». الحفظ يستدعي `listingsApi.create(form)` أو `listingsApi.update(id, form)` (إلى `POST/PUT /api/admin/listings`)، وعند الفشل يظهر «يرجى إدخال الحي والغرف والمساحة والسعر». الحذف يطلب تأكيد `window.confirm` («حذف هذا العقار؟») ثم `DELETE`. هذه العروض هي عينها الكتالوج العام في `/real-estate`.

__إدارة خدمات الخريطة (`PlacesManager`).__ بنفس النمط: جدول الأماكن من `adminPlaces.list()` (شارة الفئة + الاسم + العنوان)، و`PlaceEditor` بحقول: الاسم، الفئة (قائمة من dining/hotels/notary/hospitals/government/shopping)، خط العرض، خط الطول (مهيّأة افتراضيًا على إحداثيات إسطنبول 41.0151/28.9795)، والعنوان. الحفظ عبر `adminPlaces.create/update` (`POST/PUT /api/admin/places`)، والحذف بتأكيد «حذف هذا المكان؟». هذه الأماكن تغذّي خريطة `/map` المتاحة لـ Pro/Elite (المسار `/api/places` محميّ بـ `requirePro`).

__أسعار العملات (`Admin.tsx`).__ بطاقة «أسعار العملات» تشرح أنها تضبط سعر USD/TRY وEUR/TRY اليومي الظاهر في الشريط الأسود العلوي. تُحمَّل القيم من `adminRates.get()`. «حفظ الأسعار» (`saveRates`) يتحقق أن القيمتين موجبتان ثم `adminRates.set(usd, eur)` (`POST /api/admin/rates`)، فيتحول الزر مؤقتًا إلى «تم الحفظ» لثانيتين. «استخدام المصدر المباشر» يستدعي `adminRates.clear()` (`DELETE /api/admin/rates`) فيعود الموقع للسعر الحيّ من `/api/rates`. سطر الحالة أسفلها يعرض «تم ضبط السعر الرسمي — حُدّث {{date}}» عند وجود تجاوز يدوي، أو «يُستخدم حاليًا المصدر المباشر» خلاف ذلك. أي أن التجاوز اليدوي يَسبق المصدر الحيّ متى ضُبط.

---

## 4. حالة التنفيذ

الجدول التالي يصنّف كل ميزة بناءً على قراءة الكود الفعلي للخادم (`server/index.mjs`، `server/ai-policy.mjs`) والواجهة (`Checkout.tsx`، `Auth.tsx`، `MapPage.tsx`، `RealEstate.tsx`، `HealthTourism.tsx`، `src/lib/api.ts`) وملف `.env.example`. الرموز: ✅ مكتملة | 🟡 تعمل بوضع تطوير/تحتاج مفتاح إنتاج | ⚠️ بيانات أولية مزروعة (seed) | ❌ غير موجودة.

| الميزة | الحالة |
|---|---|
| المصادقة بالبريد: تسجيل (`/api/auth/register`)، دخول (`/api/auth/login`)، خروج | ✅ مكتملة — تجزئة كلمة المرور بـ `bcrypt`، تحقق من صحة البريد وطول كلمة المرور (≥8)، منع تكرار البريد (`email_exists`) |
| الجلسات (Sessions) | ✅ مكتملة — رمز عشوائي 32 بايت في جدول `sessions`، كوكي `rafiq_session` بصفة `HttpOnly; SameSite=Lax`، مدة افتراضية 30 يومًا، و`Secure` يُفعَّل تلقائيًا في الإنتاج (`NODE_ENV=production`) |
| تسجيل Google (الوضع الحقيقي) | 🟡 يعمل فقط عند ضبط `GOOGLE_CLIENT_ID`. عندها يُحمّل زر Google Identity Services الحقيقي ويُتحقَّق من الـ id_token عبر `tokeninfo` مع فحص `aud` و`email_verified`. حتى يُضبط المفتاح يُرجع المسار `501 google_disabled` |
| تسجيل Google (وضع التطوير) | 🟡 المسار `/api/auth/google-dev` نشط **فقط** عند غياب `GOOGLE_CLIENT_ID`؛ زر "المتابعة عبر Google" يُنشئ/يدخل المستخدم بدون تحقق حقيقي. يجب ضبط `GOOGLE_CLIENT_ID` لتعطيله إنتاجيًا |
| الصلاحيات server-side (admin) | ✅ مكتملة — `requireAdmin` يعتمد على `role` المخزّن في قاعدة البيانات؛ لا قرار ثقة في الواجهة. الأدمين يُزرع تلقائيًا عند كل إقلاع (`seedAdmin`) ويُعاد ضبط كلمة مروره من `ADMIN_PASSWORD` |
| الصلاحيات server-side (الباقة/tier) | ✅ مكتملة — `getTier` و`requirePro` يفرضان الوصول للميزات المدفوعة على الخادم؛ تنتهي صلاحية الاشتراك المنتهي تلقائيًا (تحديث الحالة إلى `expired`) |
| الاشتراكات والباقات (light/pro/elite، شهري/سنوي) | ✅ مكتملة — التفعيل، احتساب تاريخ الانتهاء (12 شهرًا للسنوي)، الإلغاء (`/api/subscriptions/cancel` مع سبب وتعليق). الأسعار ثابتة server-side: light=799، pro=1599، elite=3199 ليرة، والسنوي = الشهري × 10 |
| الدفع بالبطاقة (dev gateway + webhook) | 🟡 يعمل بمحاكي بوابة دفع. `/api/checkout/session` يُنشئ جلسة ثم يُحوّل إلى صفحة HTML للمحاكي (`/api/pay/:session`)، والتفعيل يحدث **حصرًا** عبر webhook موقّع HMAC-SHA256 يُتحقَّق منه بـ `timingSafeEqual`. لا توجد مفاتيح بوابة حقيقية؛ التعليق في الكود يشير صراحة إلى استبداله بـ Stripe/iyzico/Whop مع بقاء عقد الـ webhook كما هو. نص الواجهة الحالي يقول "تتم معالجة الدفع بالبطاقة بأمان عبر Whop" |
| التحويل البنكي / الكريبتو (دفع يدوي) | ✅ مكتملة (المسار اليدوي) — `/api/payments/manual` يسجّل الدفعة بحالة `pending`، ويُشعِر المستخدم والأدمين. بيانات الـ IBAN/المحفظة تأتي من إعدادات الخادم (`/api/checkout/config`) لا من الواجهة. ⚠️ القيم الافتراضية للـ IBAN والمحفظة في `.env.example` تجريبية ويجب استبدالها بقيم حقيقية إنتاجيًا |
| رفع الإيصالات والمستندات | ✅ مكتملة — رفع عبر `multer` بحد 10MB وقيود نوع (PNG/JPEG/WebP/PDF)، تخزين بأسماء عشوائية. خزانة المستندات (`/api/documents`) مع تنزيل وحذف محميَّين بملكية المستخدم، وإيصالات الدفع تُعرض للأدمين فقط |
| مساعد AI (المفتاح الحقيقي) | 🟡 يستدعي Anthropic Messages API فقط عند ضبط `ANTHROPIC_API_KEY` (الموديل الافتراضي `claude-haiku-4-5-20251001`)، مع بثّ SSE وتحليل ناتج JSON. حتى يُضبط المفتاح لا يعمل النموذج الحقيقي |
| مساعد AI (fallback حتمي) | ✅ مكتمل كبديل — عند غياب المفتاح يُستخدم `fallbackRespond` بردود جاهزة بأربع لغات حسب الموضوع (إقامة/ضريبة/بنك/سكن...). سياسة عرض الحجز (`policyTriggers`) تعمل في الحالتين كشبكة أمان (طلب صريح لإنسان / تكرار نفس الموضوع ≥3) |
| حدّ المعاينة المجانية للـ AI | ✅ مكتملة — `FREE_CHAT_MESSAGES` (افتراضي 3) للمستخدمين free/light عبر جدول `ai_usage`، ثم `402 upgrade_required` |
| تدفّق الحجز (Booking) | ✅ مكتملة — `/api/bookings` يتحقق من الملخص والـ transcript واللغة ورفض التاريخ الماضي، يربط الملخص ونص المحادثة تلقائيًا، ويُشعِر المستخدم والأدمين. الأدمين يدير الحالات (new/confirmed/done/cancelled) ويضيف ملاحظات داخلية |
| نظام leads (طلبات العقارات والصحة) | ✅ مكتملة — `/api/leads` بنوعين (`realestate`/`health`)، يُخزَّن ويُشعِر الطرفين. الواجهة تعكس الطلبات السابقة بعد إعادة التحميل (الزر يتحوّل إلى "تم الطلب") |
| الإحالات (Referrals) | ✅ مكتملة — رمز إحالة لكل مستخدم، تتبّع النقرات (`/api/referrals/click`)، التسجيلات والأرباح (`/api/referrals/stats`). عمولة 30% من كل دفعة مُتحقَّقة تُقيَّد مرة واحدة لكل دفعة (`INSERT OR IGNORE`) |
| الإشعارات | ✅ مكتملة — جداول إشعارات بجمهور (`user`/`admins`/`all`) وحالة قراءة لكل مستخدم (`notification_reads`)، عدّاد غير المقروء، و"قرأت الكل". الأدمين يبثّ أخبارًا عامة (`/api/admin/news`) |
| الخريطة — بوّابة Pro server-side | ✅ مكتملة — `/api/places` محمي بـ `requireAuth + requirePro`؛ المعاينة المقفلة تعرض خريطة حقيقية مضبّبة فعليًا (blur) مع دعوة للترقية. الواجهة تكتشف القفل من رمز 402/401 |
| الخريطة — نقاط الاهتمام (POIs) | ⚠️ بيانات مزروعة — 12 مكانًا (`SEED_PLACES`: مطاعم/فنادق/نوتر/مستشفيات/جهات حكومية/تسوق) تُزرع عند أول إقلاع. خريطة Leaflet/OpenStreetMap وبحث الـ "AI" يعملان بتصنيف محلي (regex) لا باستدعاء نموذج حقيقي |
| العقارات — العروض (listings) | ⚠️ بيانات مزروعة — 6 عروض (`SEED_LISTINGS`) تُزرع عند أول إقلاع، مع شارة "مؤهل للجنسية". الكتالوج العام (`/api/listings`) متاح للجميع |
| العقارات — CRUD للأدمين | ✅ مكتملة — `/api/admin/listings` (GET/POST/PUT/DELETE) مع تحقق من المدخلات (الحي/الغرف/المساحة/السعر). يقابلها CRUD مماثل لأماكن الخريطة `/api/admin/places` |
| حاسبة تكلفة المعيشة | ✅ مكتملة — مكوّن تفاعلي client-side في `BlockCard.tsx` يحسب إجماليًا حيًّا حسب (الحي مركزي/ضواحٍ × نمط budget/comfort/premium × عزْب/زوجان/عائلة). نص الواجهة: "حاسبة تكلفة المعيشة — قدّر ميزانيتك الشهرية في إسطنبول حسب الحي ونمط الحياة" |
| أسعار العملات (live) | 🟡 تعمل — `/api/rates` يجلب من `open.er-api.com` مع كاش 5 دقائق ومهلة 6 ثوانٍ. عند فشل المصدر يسقط إلى قيم ثابتة احتياطية (usdtry=41.2، eurtry=44.8). المصدر افتراضي قابل للتبديل عبر `RATES_URL` |
| أسعار العملات (تجاوز الأدمين) | ✅ مكتملة — `/api/admin/rates` (GET/POST/DELETE)؛ القيمة التي يضبطها الأدمين تتقدّم دائمًا على المصدر الحي (`source: 'admin'`) |
| الترجمة الرباعية (i18n) | ✅ مكتملة — 588 مفتاحًا بتطابق كامل عبر en/ar/ru/fa؛ النصوص المعروضة للمستخدم مترجمة فعليًا (لا أسماء مفاتيح). الاستثناءات غير القابلة للترجمة فقط (IBAN، اسم العلامة، أسماء الباقات بالروسية) |
| RTL واتجاه الواجهة | ✅ مكتملة — العربية والفارسية تعملان باتجاه RTL، مع `dir="ltr"` نقطيًا على الحقول التقنية (IBAN، السعر، المحفظة) وعنصر `DirArrow` للأسهم المتوافقة مع الاتجاه |
| الكتل الشرطية (conditional blocks) | ✅ مكتملة — سجل كتل (`registry.ts`) بأولويات ومسارات (browsing/...) يُظهِر/يخفي البطاقات حسب إجابات الـ onboarding |
| لوحة الأدمين | ✅ مكتملة — إدارة المستخدمين (مع تعيين الباقة يدويًا)، المدفوعات المعلقة (تأكيد/رفض + عرض الإيصال)، الحجوزات، الـ leads، الأخبار، تجاوز الأسعار، وCRUD العروض والأماكن — جميعها خلف `requireAdmin` |
| الصفحات القانونية (الشروط/الخصوصية/الاسترداد) | ✅ مكتملة — مسارات `/terms` و`/privacy` و`/refund` تعرض محتوى ثابتًا عبر مكوّن `Legal` |
| واتساب / wa.me / tel: / mailto: / نموذج "اتصل بنا" | ❌ غير موجودة إطلاقًا — لا يوجد أي رابط واتساب أو هاتف أو بريد أو نموذج تواصل في الكود. كل التحويل يتم عبر تدفّق الحجز وأزرار طلب الخدمة (leads) وأزرار CTA الداخلية |

### ما يحتاج مفاتيح/خدمات حقيقية ليصبح إنتاجيًا
- **بوابة دفع حقيقية**: استبدال محاكي `/api/pay/:session` بـ SDK لبوابة فعلية (Stripe/iyzico/Whop)، وضبط `PAYMENT_WEBHOOK_SECRET` بقيمة سرية طويلة بدل الافتراضي `dev-webhook-secret-change-me` (الخادم يطبع تحذيرًا عند استخدام الافتراضي).
- **`ANTHROPIC_API_KEY`**: لتشغيل مساعد AI الحقيقي بدل الـ fallback الحتمي.
- **`GOOGLE_CLIENT_ID`**: لتفعيل تسجيل Google الحقيقي وتعطيل مسار التطوير `google-dev`.
- **`ADMIN_PASSWORD`**: تغييره عن الافتراضي `admin`.
- **بيانات الدفع اليدوي** (`BANK_IBAN`، `BANK_HOLDER`، `CRYPTO_WALLET`): استبدال القيم التجريبية في `.env` بقيم حقيقية.
- **البيانات المزروعة** (6 عروض عقارية + 12 موقعًا على الخريطة): استبدالها/إثراؤها ببيانات حقيقية عبر CRUD لوحة الأدمين.
- **مصدر الأسعار**: `/api/rates` يعتمد على مزوّد مجاني (`open.er-api.com`)؛ يُنصح بمصدر موثوق أو الاعتماد على تجاوز الأدمين للإنتاج.

---

All facts confirmed. Here is the section.

## 5. اللغات الأربع

تدعم المنصّة أربع لغات كاملة: العربية (`ar`)، والإنجليزية (`en`)، والروسية (`ru`)، والفارسية (`fa`). تُعرَّف القائمة الرسمية في `src/lib/types.ts` ضمن الثابت `LANGS` ونوع `Lang = 'ar' | 'en' | 'ru' | 'fa'`، ويجري تحميل ملفات الترجمة الأربعة وتسجيلها في `src/i18n/index.ts`.

### 5.1 جدول اللغات الأربع

| اللغة | الرمز (`code`) | الاسم الأصلي (`native`) | ملف الترجمة | الاتجاه (`dir`) | عدد المفاتيح | الاكتمال مقابل الإنجليزية |
|---|---|---|---|---|---|---|
| الإنجليزية | `en` | English | `src/i18n/locales/en.json` موجود | `ltr` | 588 | المرجع (100%) |
| العربية | `ar` | العربية | `src/i18n/locales/ar.json` موجود | `rtl` | 588 | 100% (تطابق كامل) |
| الروسية | `ru` | Русский | `src/i18n/locales/ru.json` موجود | `ltr` | 588 | 100% (تطابق كامل) |
| الفارسية | `fa` | فارسی | `src/i18n/locales/fa.json` موجود | `rtl` | 588 | 100% (تطابق كامل) |

ملف الترجمة لكل لغة موجود فعليًا ويُستورد مباشرة في `src/i18n/index.ts` ويُسجَّل تحت `resources.<lang>.translation`. الملفات الأربعة متطابقة في البنية: 30 مفتاحًا أعلى المستوى متطابقة في الأربعة، وهي:
`common`, `nav`, `langSelect`, `onboarding`, `home`, `rates`, `blocks`, `auth`, `pricing`, `checkout`, `smart`, `chat`, `booking`, `map`, `referrals`, `residency`, `realEstate`, `health`, `hub`, `profile`, `notifications`, `admin`, `adminBookings`, `gates`, `legal`, `leads`, `tricks`, `notFound`, `errors`, `footer`.

### 5.2 المفاتيح المطابقة للإنجليزية (غير قابلة للترجمة)

عند مقارنة كل لغة بالإنجليزية، القيم الوحيدة المطابقة حرفيًا هي قيم تقنية/علامات تجارية لا تُترجَم بطبيعتها:

- **العربية والفارسية** — مفتاحان فقط مطابقان:
  - `checkout.bank.iban = "IBAN"` — اختصار مصرفي دولي يُكتب باللاتينية دائمًا.
  - `tricks.items.bitaksi.title = "BiTaksi / iTaksi"` — اسم علامتَي تطبيقَي سيارات الأجرة في إسطنبول؛ اسم تجاري لا يُترجَم.
- **الروسية** — ستة مفاتيح مطابقة، تشمل المفتاحين السابقين بالإضافة إلى:
  - `common.tl = "TL"` — رمز عملة الليرة التركية (يُبقى كرمز لاتيني في الروسية).
  - `pricing.light.name = "Light"`, `pricing.pro.name = "Pro"`, `pricing.elite.name = "Elite"` — أسماء الباقات التجارية الثلاث؛ تُبقى بالإنجليزية في النسخة الروسية كعلامات منتج.

أي أن التطابق مع الإنجليزية مقصود في هذه الحالات فقط، وليس ناتجًا عن ترجمة ناقصة.

### 5.3 الأقسام الناقصة الترجمة

لا توجد. التطابق (parity) كامل بين اللغات الأربع: كل المفاتيح موجودة في الملفات الأربعة بالقيمة المترجمة، ولا يوجد مفتاح متروك بقيمة إنجليزية افتراضية أو فارغ — باستثناء القيم التقنية/العلامات المذكورة أعلاه والتي لا تُترجَم أصلًا. عمليًا الترجمة مكتملة 100% لجميع الصفحات والأقسام (الرئيسية، المصادقة، الباقات، الدفع، المساعد الذكي، الدردشة، الحجز، الخريطة، الإحالات، الإقامة، العقارات، السياحة العلاجية، الحيل، المركز، الملف الشخصي، الإشعارات، لوحة الأدمين، النصوص القانونية، الأخطاء، التذييل). تجدر الإشارة إلى أن `fallbackLng: 'en'` مضبوط في `src/i18n/index.ts` كشبكة أمان فقط، لكنه لا يُستخدَم فعليًا لأي مفتاح بسبب اكتمال التطابق.

### 5.4 آلية إدارة اللغة وRTL

**تحديد الاتجاه (RTL/LTR):** في `src/i18n/index.ts` يُعرَّف الثابت `RTL_LANGS = ['ar', 'fa']`. الدالة `applyDir(lang)` تحسب الاتجاه: `rtl` إذا كانت اللغة ضمن `RTL_LANGS` (العربية والفارسية)، وإلا `ltr` (الإنجليزية والروسية)، ثم تطبّقه على عنصر `<html>` عبر:
```js
document.documentElement.lang = lang;
document.documentElement.dir = dir;
```
كما يحمل كل عنصر لغة في القائمة المنسدلة اتجاهه الخاص عبر السمة `dir={l.dir}` المأخوذة من `LANGS` في `src/components/LangSwitcher.tsx`، ليظهر اسم اللغة بمحاذاته الصحيحة.

**تبديل اللغة من الهيدر:** مكوّن `LangSwitcher` (في `src/components/LangSwitcher.tsx`) يعرض زرًا بأيقونة `globe` يحمل الاسم الأصلي للّغة الحالية (`current.native`)، نصه التلميحي `aria-label={t('common.language')}` (النص العربي الظاهر: «اللغة»). عند الفتح تظهر قائمة `<ul role="listbox">` بدلالات إمكانية وصول كاملة (`role="option"` و`aria-selected`)، مع إغلاق بالنقر خارجها أو بمفتاح `Escape`. اختيار لغة يستدعي `setLanguage(l.code)`.

**دالة `setLanguage` (المنطق المركزي):**
```js
export async function setLanguage(lang: Lang) {
  localStorage.setItem('i18nextLng', lang);
  localStorage.setItem('rafiq_lang_selected', 'true');
  await i18n.changeLanguage(lang);
  applyDir(lang);
}
```
أي أنها: تُخزّن اللغة، تعلّم أن المستخدم اختار لغة (`rafiq_lang_selected`)، تبدّل لغة i18next، ثم تعيد ضبط الاتجاه.

**التخزين في `localStorage`:** عند الإقلاع يقرأ `src/i18n/index.ts` المفتاح `i18nextLng` كقيمة مبدئية (`const stored = localStorage.getItem('i18nextLng') || 'en'`)، فيكون الافتراضي الإنجليزية عند أول زيارة. تُستخدَم هذه القيمة في `init({ lng: stored, ... })` ثم يُستدعى `applyDir(stored)` فورًا لضبط الاتجاه قبل عرض الواجهة. كما يُخزَّن `rafiq_lang_selected = 'true'` لتمييز أن المستخدم اعتمد لغة (تستفيد منه بوّابة `LanguageSelector` لتقرير ما إذا كانت ستظهر).

**الخطوط لكل لغة:** تُحمَّل خطوط Google في `index.html` (Inter، Montserrat، Tajawal، Vazirmatn، Noto Sans Arabic)، ويجري ربطها بالاتجاه/اللغة في `src/index.css`:
- الافتراضي (لاتيني، `en`/`ru`): `font-family: 'Inter', 'Montserrat', system-ui, sans-serif`.
- `html[dir='rtl']` (عربي وفارسي معًا): `'Tajawal', 'Vazirmatn', 'Noto Sans Arabic', system-ui, sans-serif`.
- `html[lang='fa']` (الفارسية تحديدًا، يتجاوز السابق): `'Vazirmatn', 'Tajawal', 'Noto Sans Arabic', system-ui, sans-serif` — أي تقديم خط Vazirmatn الأنسب للنص الفارسي.

ويعكس `tailwind.config.js` التقسيم نفسه عبر عائلتي `font-latin` و`font-arabic`.

---

## 6. التواصل والتحويل

### الفجوة الأهم: لا قناة تواصل مباشر إطلاقًا
بعد قراءة `Layout.tsx` (الهيدر والفوتر) وكل صفحات التحويل، تأكَّد أنه **لا يوجد في الموقع أي زر واتساب، ولا رابط `wa.me`، ولا روابط `tel:` للاتصال الهاتفي، ولا `mailto:` للبريد، ولا نموذج "اتصل بنا" (Contact form)**. فحص الكود أعاد صفر تطابق لأي من هذه الأنماط؛ التطابقان الوحيدان في البحث كانا كلمة `hotel` (اسم أيقونة وفئة على الخريطة) لا علاقة لهما بالتواصل.

عمليًا الفوتر في `Layout.tsx` يقتصر على: الشعار، الوسم، تنويه `footer.disclaimer` («رفيق إسطنبول خدمة إرشادية مستقلة وليست جهة حكومية.»)، روابط الخدمات الداخلية، والروابط القانونية (`/terms` و`/privacy` و`/refund`)، وسطر الحقوق `footer.rights`. لا يحتوي على هاتف ولا عنوان ولا بريد ولا أي حساب تواصل اجتماعي. النتيجة: لا يملك المستخدم أي وسيلة فورية للوصول إلى بشر خارج التدفقات الداخلية المذكورة أدناه — وهذا يضعف الثقة والتحويل لجمهور أجنبي اعتاد التواصل عبر واتساب.

### عناصر التحويل/الإجراء الموجودة فعليًا

**1. تدفّق الحجز عبر `BookingModal` (الإجراء التحويلي الأقوى)**
- **النوع:** نافذة منبثقة (Modal) لحجز موعد استشاري مجاني عبر الإنترنت.
- **المكان:** لا يظهر مستقلًا في أي مكان؛ يُستدعى حصريًا من داخل محادثة المساعد في `Premium.tsx`. عندما يُرجِع الخادم `offerBooking: true` تظهر فقاعة فيها ملخص المشكلة + نص `chat.bookOffer` («يبدو أن حالتك تستفيد كثيرًا من خبير بشري. يمكنني ترتيب موعد مجاني عبر الإنترنت...») وزر `chat.bookCta` («احجز موعدًا مجانيًا») الذي يفتح المودال.
- **ما يفعله فعلًا:** نموذج مختصر (تاريخ + وقت + لغة مفضّلة) يستدعي `bookings.create`. يُرفَق `problemSummary` ونص المحادثة كاملًا وهوية المستخدم تلقائيًا (نص `booking.attachedNote`: «يُرفَق ملخص مشكلتك ونص المحادثة تلقائيًا...»). الماضي محظور عميل-جانبيًا (`min`/`isPast`) ومرفوض خادم-جانبيًا (`past_datetime`). عند النجاح تظهر `booking.success` («تم طلب الموعد!» + «سيؤكد فريقنا موعدك عبر الإنترنت قريبًا. ستصلك إشعارات هنا.»). الحجز يصل لشاشة الأدمين `/admin/bookings`.
- **قيد مهم:** بوّابة هذا التدفق هي المحادثة، والمحادثة نفسها محمية بـ `RequireAuth` ومحدودة برسائل مجانية ثم جدار دفع (`chat.gate`) للباقات المدفوعة. أي أن أقوى مسار للوصول إلى بشري مدفون خلف خطوتين (تسجيل دخول + تصعيد المساعد).

**2. أزرار طلب الخدمة (Leads) في العقارات والصحة**
- **النوع:** أزرار CTA تُنشئ "lead" مخزَّنًا يصل للأدمين (لا تواصل فوري؛ وعد بأن يتواصل الفريق لاحقًا).
- **العقارات `RealEstate.tsx`:** زر `realEstate.cta` («اطلب التفاصيل») على كل عرض. يستدعي `leads.create('realestate', ...)` مع وصف العقار. إن لم يكن المستخدم مسجّلًا يُحوَّل إلى `/auth`. بعد الطلب يتحوّل الزر إلى حالة معطّلة بعلامة صح ويظهر `realEstate.requested` («تم إرسال الطلب! سيتواصل معك مستشارنا.»). الطلبات تُجلب مجددًا عبر `leads.mine()` لتبقى الحالة بعد إعادة التحميل.
- **الصحة `HealthTourism.tsx`:** زر `health.cta` («احصل على عرض سعر مجاني») لكل خدمة (شعر/أسنان/فحص). يستدعي `leads.create('health', ...)`، ثم يعرض `health.requested` («تم إرسال طلب العرض! سنرد خلال 24 ساعة.»). السلوك نفسه: تحويل إلى `/auth` لغير المسجّلين، وحالة محفوظة عبر `leads.mine()`.
- **ملاحظة:** هذان المساران يَعِدان صراحةً بأن «يتواصل معك مستشارنا» و«نرد خلال 24 ساعة»، لكن لا يوجد في الكود قناة يُبادر منها المستخدم بالتواصل — كل المبادرة تقع على فريق الأدمين.

**3. روابط مشاركة الإحالة في `Referrals.tsx`**
- **النوع:** نسخ رابط دعوة شخصي (مشاركة عبر الوسائط الخارجية يدويًا، لا أزرار مشاركة جاهزة).
- **المكان:** صفحة `/referrals` (محمية بـ `RequireAuth`).
- **ما يفعله فعلًا:** يبني الرابط `${origin}/r/${code}` ويعرضه في حقل `<code>`، وزر يستدعي `navigator.clipboard.writeText(link)` ويتحوّل من `common.copy` («نسخ») إلى `common.copied` («تم النسخ») لمدة 1.5 ثانية. النصوص: `referrals.title` («ادعُ واربح») و`referrals.subtitle` («شارك رفيق مع أصدقائك واربح 30% من كل دفعة يسددونها.»). لا يوجد زر "شارك عبر واتساب/تيليجرام"؛ المشاركة تتم بالنسخ واللصق يدويًا فقط.

**4. أزرار CTA الداخلية في الكتل (`BlockCard.tsx`) وروابط "حمّل التطبيق" (`Tricks.tsx`)**
- **كتل `BlockCard.tsx`:** ثلاثة أنماط من الزر السفلي حسب `block.kind`:
  - نمط `nudge`: زر يستدعي `onNudge` (يحدّث مسار البروفايل ثم ينتقل إلى `/`).
  - بقية الأنواع (عدا `services` و`col`): `<Link>` إلى `block.ctaTo` بنص `blocks.{id}.cta` — وهي الأزرار التي تقود غالبًا إلى `/premium` أو الصفحات الخدمية الداخلية.
  - كتل `services` و`col` لا تعرض زرًا (تعرض شبكة الخدمات/حاسبة المعيشة بدلًا منه). شبكة الخدمات روابط داخلية إلى `/residency` و`/real-estate` و`/health-tourism` و`/map`.
- **حيل `Tricks.tsx`:** بطاقات نوع `app` تعرض **رابطًا خارجيًا حقيقيًا** `<a href target="_blank" rel="noreferrer">` بنص `tricks.openApp` («حمّل التطبيق») يفتح المواقع الرسمية للتطبيقات: `turkiye.gov.tr` (e-Devlet)، `e-ikamet.goc.gov.tr`، `istanbulkart.istanbul`، `bitaksi.com`، `getir.com`. هذه روابط خارجية للتطبيقات الرسمية فقط، وليست قنوات تواصل مع رفيق. وفي أسفل الصفحة بطاقة CTA بنص `tricks.ctaTitle` («محتار في أمر معيّن؟») وزر `tricks.ctaButton` («اسأل المساعد») يقود إلى `/premium`.

**5. شريط الإشعارات / الجرس (`Layout.tsx`)**
- **النوع:** أيقونة جرس في الهيدر تقود إلى `/notifications`، مع شارة عدد غير المقروء (`unread`).
- **ما يفعله فعلًا:** `<Link to="/notifications">` بأيقونة `bell` و`aria-label` من `nav.notifications`؛ عند `unread > 0` تظهر شارة حمراء بالعدد. هذه قناة **داخلية أحادية الاتجاه** (المنصّة → المستخدم) لإبلاغه بتأكيد المواعيد وردود الـ leads؛ ليست وسيلة تواصل صادرة من المستخدم. للأدمين فقط يظهر إضافيًا عدّاد `newBookings` على رابط `/admin/bookings` يُجلب عبر `bookings.newCount()`.

### خلاصة الفجوات (توصية موجزة)
- **غياب كامل لقنوات التواصل المباشر:** لا واتساب، لا هاتف (`tel:`)، لا بريد (`mailto:`)، لا نموذج "اتصل بنا". لجمهور أجنبي يعتمد واتساب كقناة افتراضية، هذه أبرز فجوة تحويلية في الموقع.
- **كل مسارات الوصول إلى بشري مشروطة بتسجيل الدخول** (`RequireAuth` على المحادثة والإحالات؛ وتحويل أزرار الـ leads إلى `/auth`)، والحجز مدفون خلف تصعيد المساعد داخل المحادثة المحدودة بجدار دفع.
- **أزرار الـ leads تَعِد بمعاودة الاتصال** («سيتواصل معك مستشارنا»، «نرد خلال 24 ساعة») دون أن تمنح المستخدم أي قناة استباقية لمتابعة طلبه بنفسه.
- **مشاركة الإحالة يدوية بالكامل** (نسخ/لصق) بلا أزرار مشاركة مباشرة لتطبيقات المراسلة، ما يقلّل معدل انتشار الدعوات.

التوصية: إضافة قناة تواصل مباشرة واحدة على الأقل (زر واتساب عائم أو في الفوتر، أو بريد/نموذج تواصل) تكون متاحة قبل تسجيل الدخول، مع أزرار "شارك عبر واتساب/تيليجرام" في صفحة الإحالات.

---

## 7. البنية التقنية

تتكوّن منصّة Rafiq Istanbul من طبقتين منفصلتين: **واجهة React/Vite** تعمل في المتصفح، و**خادم Express + SQLite** هو مصدر الحقيقة الوحيد. تتواصل الطبقتان حصريًا عبر طبقة API رفيعة (`src/lib/api.ts`) ونقاط نهاية تحت المسار `/api`.

### 7.1 المكتبات المستخدمة

تُقرأ من `package.json` (الإصدارات كما هي مُثبّتة في الملف):

**اعتماديات التشغيل (`dependencies`):**

| المكتبة | الإصدار | الدور |
|---|---|---|
| `react` / `react-dom` | ^18.3.1 | نواة الواجهة وعرضها في DOM. |
| `react-router-dom` | ^6.28.0 | التوجيه من جانب العميل (`BrowserRouter`، `Routes`، `lazy`). |
| `i18next` | ^23.16.8 | محرّك الترجمة وإدارة الموارد اللغوية. |
| `react-i18next` | ^15.1.3 | ربط i18next بمكوّنات React عبر `useTranslation`/`t()`. |
| `react-leaflet` | ^4.2.1 | عرض خريطة الخدمات التفاعلية (صفحة `/map`). |
| `leaflet` | ^1.9.4 | محرّك الخرائط الأساسي الذي يبني عليه react-leaflet. |
| `lucide-react` | ^1.17.0 | مجموعة الأيقونات المتجهة المستخدمة في الواجهة. |
| `express` | ^5.2.1 | إطار خادم HTTP لكل نقاط `/api`. |
| `bcryptjs` | ^3.0.3 | تجزئة كلمات المرور (hash/compare) بدون اعتماديات أصلية. |
| `multer` | ^2.1.1 | استقبال رفع الملفات (الإيصالات والمستندات) إلى القرص. |
| `dotenv` | ^17.4.2 | تحميل متغيّرات البيئة من `.env` عند الإقلاع. |

> ملاحظة: قاعدة البيانات تستخدم وحدة Node المدمجة `node:sqlite` (`DatabaseSync`)، لذا لا تظهر مكتبة SQLite في الاعتماديات — لا حاجة لاعتمادية أصلية.

**اعتماديات التطوير (`devDependencies`):** `vite` (^5.4.11) و`@vitejs/plugin-react` (أداة البناء وخادم التطوير)، `typescript` (~5.6.3)، `tailwindcss` (^3.4.15) مع `postcss` و`autoprefixer` (نظام التنسيق)، `concurrently` (^10.0.3) لتشغيل الواجهة والخادم معًا عبر `dev:all`، إضافة إلى حِزَم الأنواع `@types/*` لـ react وexpress وleaflet وmulter وbcryptjs.

**سكربتات التشغيل:** `dev` (واجهة Vite)، `dev:server` (خادم بوضع `--watch`)، `dev:all` (الاثنان معًا)، `build` (`tsc -b && vite build`)، `test` (اختبارات الخادم)، و`check:i18n` (فحص تطابق الترجمات).

### 7.2 المعمارية: الواجهة وطبقة api.ts والخادم

**الواجهة و`api.ts` كـ fetch client رفيع:** الملف `src/lib/api.ts` هو الوسيط الوحيد بين الواجهة والخادم. الدالة الداخلية `req<T>()` تغلّف `fetch` فقط، تُرسل دائمًا `credentials: 'include'` (لتمرير كوكي الجلسة)، وتضبط `content-type: application/json` ما لم يكن الجسم من نوع `FormData` (حالات الرفع). أي خطأ من الخادم يُحوَّل إلى `ApiError` يحمل `code` آليًا قابلًا للقراءة و`status`. التعليق الافتتاحي للملف ينصّ صراحةً على أن "كل قرارات الثقة (الهوية، دور الأدمين، الباقة المدفوعة، تفعيل الدفع) موجودة في الخادم؛ هذه الوحدة تنقل البيانات فقط". الوحدة منظَّمة كمجموعات مُسمّاة: `auth`، `checkout`، `bookings`، `notifications`، `documents`، `leads`، `referrals`، `listings`، `places`، `adminUsers`، `adminPayments`، `adminRates`، `adminPlaces`، إضافة إلى `ai.chat` (دردشة تدفّقية SSE) و`fetchRates`.

**الخادم Express + SQLite كمصدر الحقيقة:** يُبنى التطبيق في `server/index.mjs` عبر مصنع `createApp(db, cfg)` يفصل الإعداد عن التشغيل (يُستفاد منه في الاختبارات). آليات الثقة الأساسية:

- **جلسات httpOnly:** عند تسجيل الدخول يُنشأ توكن عشوائي (`crypto.randomBytes(32)`) ويُخزَّن في جدول `sessions`، ويُرسَل في كوكي `rafiq_session` بخصائص `HttpOnly; SameSite=Lax`، ويُضاف `Secure` تلقائيًا في الإنتاج (`NODE_ENV === 'production'`). وسيط `attachUser` يقرأ الكوكي ويربط المستخدم بكل طلب، يليه `requireAuth` و`requireAdmin` و`requirePro` للتحكم بالصلاحيات.
- **bcrypt:** كلمات المرور تُجزّأ عبر `bcrypt.hash(..., 10)` وتُقارن بـ `bcrypt.compare`؛ لا تُخزَّن كلمة مرور خام إطلاقًا.
- **multer للرفع:** تخزين على القرص (`diskStorage`) بأسماء عشوائية، بحدّ حجم 10MB، وفلتر أنواع يقبل `png/jpeg/webp/pdf` فقط، يُستخدم لرفع إيصالات الدفع والمستندات.
- **webhook موقّع:** نقطة `/api/webhooks/payment` تُركَّب قبل `express.json` لتلقّي الجسم الخام، وتتحقق من توقيع HMAC-SHA256 عبر `crypto.timingSafeEqual` (مقارنة ثابتة الزمن). تفعيل الاشتراك يمرّ حصريًا عبر الدالة الموحَّدة `settleVerifiedPayment` (idempotent)، التي يستدعيها كلٌّ من الـ webhook والتحقق اليدوي من الأدمين، وتُسجّل أيضًا عمولة الإحالة (30% من قيمة الدفعة، مرة واحدة لكل دفعة).

**جداول قاعدة البيانات الرئيسية** (من `server/db.mjs`، تُنشأ عبر `CREATE TABLE IF NOT EXISTS` مع `PRAGMA journal_mode = WAL`، وملف القاعدة `server/data/rafiq.db`):

`users` (الحساب والدور والكود الإحالي ومن أحال صاحبه)، `sessions` (التوكنات وتواريخ الانتهاء)، `profiles` (بيانات الملف الشخصي كـ JSON)، `subscriptions` (الباقة والدورة والحالة وتواريخ البدء/الانتهاء)، `payments` (الدفعات بطريقتها وحالتها ومسار الإيصال وجلسة البوابة)، `bookings` (الحجوزات مع ملخّص المشكلة والـ transcript والموعد المفضّل والملاحظة الداخلية)، `notifications` و`notification_reads` (الإشعارات مع حالة قراءة لكل مستخدم)، `documents` (خزنة مستندات المستخدم)، `leads` (طلبات العقارات/الصحة)، `referral_clicks` و`referral_credits` (نقرات وأرصدة الإحالة)، `ai_usage` (عدّاد رسائل الدردشة المجانية لكل مستخدم)، `listings` (كتالوج العقارات)، `places` (نقاط الخريطة)، و`settings` (إعدادات عامة كـ key/value، مثل تجاوز أسعار العملات `rates_override`). كل صفّ يُولَّد له معرّف عبر `uid(prefix)` وطابع زمني ISO عبر `now()`.

### 7.3 إدارة اللغة و RTL تقنيًا

التهيئة في `src/i18n/index.ts`:

- يُحمَّل قاموس واحد لكل لغة (`en/ar/ru/fa`) كموارد `translation`، مع `lng` مقروء من `localStorage` (المفتاح `i18nextLng`، الافتراضي `en`)، و`fallbackLng: 'en'`، و`escapeValue: false` (لأن React يهرّب القيم أصلًا)، و`returnNull: false`.
- اللغات ثنائية الاتجاه معرَّفة في `RTL_LANGS = ['ar', 'fa']`. الدالة `applyDir(lang)` تضبط `document.documentElement.lang` و`dir` (إلى `rtl` للعربية والفارسية، و`ltr` لغيرهما) — أي أن RTL يُطبَّق على مستوى عنصر `<html>` فيتكفّل CSS (وخصائص Tailwind المنطقية مثل `ms-*`/`me-*`/`start-*`/`end-*` المستخدمة في `Layout.tsx`) بانعكاس التخطيط تلقائيًا.
- الدالة `setLanguage(lang)` تحفظ اللغة وعلامة `rafiq_lang_selected` في `localStorage`، تستدعي `i18n.changeLanguage`، ثم `applyDir` لتحديث الاتجاه فورًا.
- عنوان الصفحة والوصف (meta description) يُحدَّثان حسب اللغة في `Layout.tsx` ضمن `useEffect` يعتمد على `i18n.language`.

### 7.4 التحميل الكسول وErrorBoundary والشريط الأسود وإعداد Vite

**التحميل الكسول (`lazy` + `Suspense`):** في `src/App.tsx` صفحة `Home` فقط مستوردة بشكل ثابت لإبقاء الإقلاع سريعًا، بينما كل المسارات الثانوية (Auth، Pricing، Checkout، Smart، Premium، MapPage، Hub، Admin… إلخ) مُحمَّلة عبر `lazy(() => import(...))` وملفوفة بـ `<Suspense fallback={<Spinner />}>`. هذا يقسّم الحزمة (code-splitting) كما يوضّح التعليق `P3-7`.

**ErrorBoundary:** المكوّن الجذري `App` يلفّ كامل الشجرة بـ `<ErrorBoundary>` ثم `<AppProvider>` ثم `<BrowserRouter>`، فيلتقط أي خطأ عرض دون انهيار التطبيق. كما تظهر بوّابة `LanguageSelector` قبل اختيار اللغة، و`OnboardingModal` إن لم يكتمل الـ onboarding (داخل `Shell`).

**الشريط الأسود `TopRatesBar`:** شريط رفيع مثبَّت أعلى الهيدر في `Layout.tsx`، يعرض سعري **USD/TRY** و**EUR/TRY** مباشرةً. يجلب الأسعار من `/api/rates` عبر `fetchRates()` ويُحدّثها كل 5 دقائق (`setInterval`). يُظهر مؤشّر حالة: نقطة خضراء نابضة عند المصدر المباشر (`live`)، وكهرمانية خلاف ذلك، مع نص الحالة من الترجمة — "أسعار مباشرة" (`rates.live`) أو "أسعار محفوظة" (`rates.cached`) أو "السعر الرسمي" (`rates.official`) حين يكون المصدر `admin`. عنوان الشريط هو "أسعار العملات" (`rates.label`)، وكتلة الأرقام تُجبَر على `dir="ltr"` لعرض الأسعار سليمة حتى في الوضع RTL.

**إعداد Vite (`vite.config.ts`):** المكوّن `@vitejs/plugin-react`، وخادم تطوير على المنفذ `5173` بـ `host: true` (ربط على `0.0.0.0` لإتاحته لأجهزة الشبكة المحلية). `allowedHosts` يسمح بنطاقات الأنفاق العامة (`.trycloudflare.com`, `.ngrok-free.app`, `.ngrok.app`, `.ngrok.io`). وأهمّها **proxy**: كل طلب إلى `/api` يُمرَّر إلى خادم API على `http://localhost:8787` — ما يسمح للجهاز الذي يفتح Vite بالوصول إلى الخادم دون إعدادات CORS.

### 7.5 ملاحظات معمارية مهمة

- **كل قرارات الثقة في الخادم:** الواجهة لا تتخذ أي قرار أمني. الهوية (الجلسة)، صلاحية الأدمين (`role === 'admin'`)، الباقة المدفوعة (`getTier` المحسوب من جدول `subscriptions`)، وتفعيل الدفع — جميعها تُفرَض في الخادم عبر الوسطاء (`requireAuth`/`requireAdmin`/`requirePro`) ومنطق الدفع. مثلًا، نقطة `/api/places` (نقاط الخريطة الكاملة) محميّة بـ `requirePro` فلا تصل إلا لباقات Pro/Elite بقرار خادمي، وحدّ رسائل الدردشة المجانية مُطبَّق عبر جدول `ai_usage` لا عبر الواجهة.
- **سكربت فحص تطابق الترجمات:** `scripts/check-i18n-parity.mjs` (يُشغَّل عبر `npm run check:i18n`) يسطّح مفاتيح اللغات الأربع ويفشل بخروج 1 إن وُجد مفتاح ناقص أو زائد في أي لغة مقابل الإنجليزية، ويطبع عند النجاح "i18n parity OK" مع عدد المفاتيح — ما يضمن بقاء التطابق الكامل عمليًا.
- **اختبارات الخادم:** `server/test.mjs` (تُشغَّل عبر `npm test` / `node --test`) اختبارات تكامل حقيقية: تُنشئ التطبيق بقاعدة بيانات في الذاكرة (`openDb(':memory:')`) ومُستمع HTTP على منفذ عشوائي، مع عميل fetch يلتقط/يرسل كوكي الجلسة — فتُختبر سلاسل الطلب الفعلية لا الدوال منعزلة.
- **أوضاع التطوير (placeholder صريحة):** الخادم يُحذّر عند الإقلاع إذا استُخدم `PAYMENT_WEBHOOK_SECRET` الافتراضي أو غاب `ANTHROPIC_API_KEY`؛ الدفع بالبطاقة يمرّ عبر "dev gateway simulator" (`/api/pay/:session`) الذي يحاكي صفحة الدفع المستضافة ثم يُسلّم webhook موقّعًا بنفس عقد البوابة الحقيقية؛ ومساعد الـ AI يستخدم fallback حتميًا ما لم يُضبط مفتاح Anthropic. هذه الأوضاع معزولة ومُعلَّمة بوضوح في الكود بحيث يكفي ضبط متغيّرات البيئة لاستبدالها بالخدمات الحقيقية دون تغيير المعمارية.

